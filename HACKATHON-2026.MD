# 黑客松包装文案

## 推荐定位

**作品名：Rive 动效协作台**  
**对外副标题：钉钉会话里的动效评审室**  
**核心口号：别再发录屏了。一个链接，直接看 Rive。**

这不是“又一个播放器”，而是把 `.riv` 文件变成一个可播放、可交互、可评论、可下载的在线评审页。设计师把链接发到钉钉，业务和研发点开就能看到同一个真实动效，反馈也留在当前文件下面。

> 当前实现是适合在钉钉会话中分享的 H5，不宣传为钉钉原生微应用、机器人或钉钉登录产品。

## 最新版产品介绍（2026-08-26）

**Rive 动效协作台是一款面向动效设计、研发联调和业务验收的在线 `.riv` 预览与协作工具：上传文件即可生成云端短链，在手机、平板和电脑上随时查看真实动效、调试交互、评论反馈并下载源文件。**

### 1. 云端高速托管，分享链接快速打开

文件导入后先在本机立即预览，同时在后台上传云端，不用等上传结束才开始检查。上传完成会生成简短公开链接；小文件接近秒开，`17,590,968 B` 的真实文件公网完整下载实测约 `1.75s / 10.05 MB/s`。切换云端文件也不会刷新整个页面，下载和解析进度只出现在预览画布内，其他检查内容保持原位。

### 2. 手机、平板、电脑无缝适配

同一个链接会根据屏幕自动调整布局：手机上优先保证动效、播放控制和评论可触达；平板使用舒展的上下布局；大屏电脑提供文件、预览和检查区并行的工作台。无论在公司评审、在家补看，还是路上用手机确认，都不需要安装专业软件或重新传文件。

### 3. Rive 官方同源渲染，直接还原真实文件

平台使用 Rive 官方 `@rive-app/webgl2-advanced@2.39.1` 与 `@rive-app/canvas-advanced@2.39.1` 运行时，直接解析原始 `.riv`，不是录屏、抽帧或视频转码。WebGL2 负责优先呈现更接近设计稿的光影与细节，环境不支持时自动切换 Canvas2D 兼容模式，动画仍可继续播放。JOJO 托管版默认保持平衡画质，并对复杂文件自动保护性能。

### 4. 不只看画面，还能检查完整交互

画板、状态机、时间轴、状态机输入、声音、缩放方式、背景和渲染质量都能直接切换；事件控制台会记录播放、状态切换和 Rive Event，实时 FPS 与时间轴进度同步显示。业务看最终效果，动效师查动画，研发核对交互逻辑，可以围绕同一个文件完成验收。

### 5. 评论跟着文件走，反馈不再散落

每个云端短链都有独立评论区，反馈天然归属于当前文件，不用回到群聊里猜“说的是哪一版”。普通回车即可发表，`Shift + Enter` 或 `Command + Enter` 换行；评论可以归档和恢复，文件归档后也保留原链接与历史内容，恢复后继续使用。

### 6. 时间轴可以展开，也可以自动整理

少量时间轴直接平铺；超过 `10` 条时默认进入“整理”模式，按照名称中的下划线前缀自动分组，只显示更简洁的后缀，同时保留完整名称。用户可以随时切回“展开”，组标题与当前内容保持清楚。写评论时点击时间轴，还能把完整名称直接引用进正文，发布后点击引用即可切回对应动画。

### 7. 面向真实大文件和复杂素材设计

公开文件采用真实字节流进度，不用假进度掩盖网络等待；连接超时或传输停滞会明确提示并允许重试。复杂素材会根据体积、画板、动画和状态机数量自动限制 DPR、帧率或音频解码，避免为了追求高清让页面失去响应。项目已用从几十 KB 到 `55 MB` 的真实 Rive 文件验证播放和切换稳定性。

### 8. 从上传到验收，保持同一个版本

最近文件把本机素材、云端文件和浏览器看过的公开文件统一管理；文件可以下载、复制链接、归档和恢复，不提供不可恢复的永久删除。它把“上传 → 分享 → 查看 → 评论 → 下载”的协作链路收进同一个页面，让动效交付不再依赖反复录屏和散落的群消息。

## 一句话介绍备选

1. 发一个链接，让设计、业务和研发看到同一个会动的版本。
2. 不再用录屏交付动效，让 Rive 文件自己开口说话。
3. 把 Rive 文件变成一个能看、能评、能下载的在线评审室。
4. 钉钉里点开链接，直接预览真实 Rive 动效和交互。
5. 让动效被看见，也让每一条反馈有地方落下。
6. 从文件交付到共同验收，只差一个链接。

## 大众评委最容易感知的 6 个卖点

### 1. 痛点一秒就懂

以前交付 Rive 动效，设计师往往要额外录一段视频。视频只能“看”，不能点击、切状态，也很快会和最新版文件对不上。

### 2. 发链接就能看

上传 `.riv` 后生成短链接。对方无需安装专业软件、无需注册账号，手机和电脑都能直接打开。

### 3. 看到的是真实动效

页面运行的是完整 Rive 文件，不是压扁后的 MP4。播放、倍速、画板、状态机、时间轴和输入仍然存在。

### 4. 反馈不再散落

每个分享链接都有自己的评论区。评论跟着文件走，不用回到群里猜“这句话说的是哪一版、哪一帧”。

### 5. 一页完成评审闭环

预览、事件、FPS、评论、下载和分享都在同一个页面里。业务看效果，研发看交互，动效师收反馈，各取所需。

### 6. 不是概念图，是可用产品

真实公开页已经可以在手机和电脑访问，公开样例为 <https://rive.mikeywa.site/vGd>。页面里能直接看到绿豆动效、播放控制、事件记录、实时 FPS、评论与下载入口。

## 报名简介

### 约 80 字

Rive 动效协作台把 `.riv` 文件变成可播放、可交互、可评论、可下载的在线评审页。设计师把短链接发到钉钉，业务和研发无需安装软件、无需注册，点开就能看到同一个真实动效，不再靠录屏猜版本。

### 约 180 字

动效交付经常卡在最后一步：设计师做的是可交互的 Rive 文件，发给业务和研发时却只能录成视频。交互丢了、版本乱了，反馈还散落在群聊里。Rive 动效协作台把原始 `.riv` 文件变成一个在线评审页：上传后生成短链接，在钉钉里发出去，对方无需安装和注册即可预览、播放、切换状态、查看事件与 FPS、评论并下载原文件。每个链接都有独立评论区，归档后还能恢复原链接，让“发文件”真正变成“共同验收”。

### 约 350 字

Rive 动效是会响应点击、状态和数据的交互资产，但在真实协作中，它经常被压扁成一段录屏：业务只能看结果，研发看不到状态机和事件，动效师还要在钉钉群里反复解释版本。

Rive 动效协作台把一个 `.riv` 文件变成独立的在线评审室。动效师上传文件后得到短链接，发到钉钉即可分享；查看者无需安装专业软件、无需注册账号，手机和电脑点开就能看到真实动效。页面同时保留播放与倍速、画板、状态机、时间轴、输入、事件记录和实时 FPS，并把评论、下载与分享放在同一条链路中。每个链接拥有独立评论线程，文件可以归档和恢复，原链接与历史反馈继续保留。

它解决的不是“能不能播放”这一小步，而是让设计、业务和研发围绕同一个真实版本完成预览、反馈和验收。真实样例：<https://rive.mikeywa.site/vGd>。

## 评委版表达

### 面向大众评委

如果您曾收到过“最终版_v7_真的最终版.mp4”，这个产品就是为了解决它。动效师不用再录屏，发一个链接，您点开就能看到真正会动、能交互的版本，还能直接留下反馈。

### 面向专业评委

产品没有把 Rive 当视频播放，而是保留完整运行时和交互语义；同一页面同时承载画板、状态机、时间轴、输入、事件与播放遥测。复杂文件按体积和运行复杂度自动限制 DPR、帧率与音频解码，前端高频 FPS/时间轴更新与根组件隔离，公开文件使用独立评论线程、流式下载进度和可恢复归档。

### 面向业务评委

它缩短的是动效资产从“制作完成”到“跨角色验收”的最后一公里：少录一次视频、少解释一次版本、少丢一条反馈，也让原始交付物始终可追溯、可下载、可继续协作。

## 30 秒路演话术

大家平时看到动效师发在群里的，往往不是原文件，而是一段录屏。因为业务没装 Rive，研发也不一定能马上打开，所以一个本来能交互的动效，交付时先被压扁成了视频。

我做的 Rive 动效协作台，就是把这一步去掉。上传 `.riv`，生成一个短链接，发到钉钉；对方点开就能看真实动效、看事件和 FPS、直接评论，也能下载原文件。现在屏幕上的绿豆不是视频，它就是正在运行的 Rive。对我来说，最重要的变化是：从“我发给你看”变成“我们围绕同一个版本一起评”。

## 现场演示顺序

1. 先展示群里常见的录屏，问一句：“如果要改的是点击反馈，您能从视频里点出来吗？”
2. 打开 <https://rive.mikeywa.site/vGd>，让绿豆角色真实播放。
3. 操作播放、倍速或画布，让评委确认它不是视频。
4. 展开事件条，指出页面能看到 `PLAY / idle / FPS` 等运行信息。
5. 滚到“评论与备注”，说明反馈归属于当前文件。
6. 最后点一下黄色“复制分享链接”，落到口号：“从文件交付到共同验收，只差一个链接。”

## 拉票文案

### 极短版

别再发录屏了。一个链接，直接看 Rive。请把一票投给「Rive 动效协作台」。

### 群聊版

我把动效交付里最麻烦的一步做掉了：不用录屏、不用装软件，钉钉里发一个链接，对方点开就能看真实 Rive、留评论、下原文件。如果您也受够了“最终版_v7_真最终版”，欢迎把一票投给「Rive 动效协作台」。

### 朋友圈版

动效不该在交付时被压扁成一段录屏。  
我做了一个 Rive 在线评审台：上传 → 分享 → 一起评。  
真实样例：<https://rive.mikeywa.site/vGd>  
如果这个小工具让您觉得“确实该有”，欢迎投我一票。

## 标题和口号备选

- 动效直达｜发一个链接，一起看 Rive
- Rive 动效协作台｜钉钉会话里的动效评审室
- 别再发录屏了｜让 Rive 文件自己开口说话
- 动效评审室｜每个文件都有自己的反馈现场
- 一个链接，看见真实动效
- 让动效被看见，让反馈有地方落下
- 从文件交付到共同验收，只差一个链接
- 不只看画面，还能看懂交互

## 如果报名表要求说明 AI

建议如实写：AI 不是为了凑功能而放进页面的聊天框，而是作为研发协作者参与需求拆解、跨端运行时适配、性能策略、测试和发布验证。最终产物是已上线、可真实打开和协作的工具，而不是只在演示里成立的概念 Demo。

不要写“AI 自动生成 Rive”“AI 自动理解评论”或“钉钉原生应用”，因为当前产品没有这些用户功能。

## 6 张封面使用建议

| 文件 | 主打角度 | 推荐场景 |
| --- | --- | --- |
| `01-stop-recording.png` | 痛点反差 | 大众投票首选 |
| `02-dingtalk-link.png` | 钉钉分享、手机直达 | 强调参赛版本 |
| `03-one-page-review.png` | 一页评审闭环 | 作品详情页 |
| `04-feedback-lands.png` | 情绪价值、反馈归位 | 社交传播 |
| `05-one-link-workflow.png` | 流程极简 | 路演/PPT |
| `06-understand-interaction.png` | 专业能力 | 专业评委与技术答辩 |

首选推荐：大众评委用 `01`，报名详情用 `03`，专业答辩用 `06`。
