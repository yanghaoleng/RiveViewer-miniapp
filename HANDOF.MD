# 当前交接

更新日期：2026-08-26

## 2026-08-26 三套 H5 通用体验数据后台（已发布）

- 新入口 <https://rive.mikeywa.site/data/> 汇总 H5 通用版、叫叫正式版和叫叫测试版；默认查看叫叫正式版，自绘周期、版本和格式下拉支持鼠标、方向键、Home、End、回车、空格与 Escape。已移除 HTTP Basic Auth 和账号输入，改为单一六位密码锁屏；支持屏幕数字键盘、实体数字键、退格、回车和 Escape 清空。
- 密码仅配置在服务端 `RIVE_HOST_ANALYTICS_PASSWORD`，不进入前端源码或构建包。正确密码签发 `HttpOnly + Secure + SameSite=Strict` 的 12 小时签名 Cookie；汇总 API 必须校验登录态，登录接口按 IP 限制为每分钟 `10` 次并允许短时 `5` 次突发。
- 核心口径覆盖独立会话、成功预览、激活率、加载成功率、P95 首帧、深度使用、错误率、复访率、可见停留；趋势区新增近 `7 / 30 / 90` 天的独立用户数与访问次数。已删除顶部大标题、解释文案和三版本对比表，“问题维度”改名为“访问来源”。
- 加载成功率出现失败时可展开具体托管文件并直接打开复测。统计日志只存公开三位短码与有界失败分类，不存文件名；汇总接口用现有托管目录核验短码并补当前文件名，不存在或已归档的记录不会展示。
- 埋点按批发送，服务端只存随机标识哈希、粗粒度客户端和行为结果；不保存原始 IP、完整 User-Agent、文件名、文件内容或评论正文。采集路径关闭 Nginx access log，数据保留 `90` 天，并尊重浏览器 DNT/GPC。
- 数据后台为 `/data/` 独立构建，当前压缩入口脚本约 `60.65 KB gzip`，数据页脚本约 `6.63 KB gzip`、样式约 `3.64 KB gzip`，不包含 Rive/Lottie/PAG 播放器分包。
- H5 数据构建 `49/49` 自动测试通过、`1` 项播放器分包测试按设计跳过，服务端 `13/13` 通过，`npm audit = 0`。本地浏览器已验证默认正式版、失败文件展开、自绘菜单视觉与键盘选择，密码值未出现在前端构建产物。
- 公网浏览器已验证实体键盘数字输入与回车提交、虚拟键盘输入和删除、错误密码反馈；`390 × 844` 下页面宽度与视口一致，卡片左右各留 `14px`，按键高 `54px`，无控制台错误。
- 数据后台、H5 通用版、叫叫正式版、叫叫测试版和后端已统一原子切换为 `20260826-151015`；旧静态与后端软链接均保留为直接回滚点。
- 完整数据备份为 `/var/backups/rive-host-data-before-20260826-142218.tar.gz`；发布前后 `state.json` SHA-256 均为 `bbdf161b76ef2f3c0977763862929a7b67d9188a64e58042224be3e656191fba`，11 份文件未变。第一次切换因后端发布包目录层级不符被健康闸门自动回滚；修正后再切换成功。

## 2026-08-26 Beta Lottie / PAG（已发布）

- 本轮只扩展 H5 Beta，小程序与正式根路径静态前端未改。Beta 现支持 `.riv`、自包含 Lottie `.json` 和 `.pag`，统一保留播放、重播、倍速、进度、FPS、完整/铺满、背景和评论；Rive 独有的画板、状态机、输入、事件与渲染引擎只在 Rive 文件显示。
- PAG 上限严格为 `10 MiB`，浏览器与服务端双重校验：测试中 `10 MiB` 通过，`10 MiB + 1 B` 在线接口返回 `413 file_too_large`。Lottie 拒绝外链图片或字体，只允许自包含 JSON，避免预览时向第三方地址发起不可控请求。
- 三个运行时均动态加载；首屏入口为 `89.91 KB gzip`，Lottie 首次打开增量约 `57 KB gzip`，PAG 首次打开增量约 `700 KB gzip`（含 `609.93 KB gzip` WASM）。已移除首屏 Rive WASM 预加载，非 Rive 用户不再承担该带宽。
- H5 lint、TypeScript、`/beta/` 构建、`46/46` 测试和 `npm audit = 0` 均通过；服务端 `12/12` 通过。真实浏览器使用 Rive Guide、Airbnb Lottie 示例和 Tencent libpag 示例完成回归：Lottie 约 `60 FPS`，PAG 约 `24 FPS`，Rive 原控制保持正常，三者均无控制台错误或警告；PAG 在 `390×844` 手机视口无横向溢出。
- 线上测试链接为 Lottie <https://rive.mikeywa.site/beta/V1e>（`1,959,221 B`）与 PAG <https://rive.mikeywa.site/beta/lOF>（`6,922 B`）。Beta 列表显式请求三种格式；正式列表不带格式参数时只返回 Rive。Beta 另用 IndexedDB `rive-viewer-h5-beta`，不会污染正式页最近记录。
- Beta 前端与兼容后端已原子切换为 `20260826-132255`；正式前端仍为 `20260826-111929`。公网 Beta HTML 与本机构建 SHA-256 均为 `a205f4fc6770f62842d72efc7c7d3824a893caed4f9e80406188fa9afb250206`，PAG WASM 为 `application/wasm` 且一年 immutable；API PID 为 `265276`。
- 当前完整数据备份为 `/var/backups/rive-host-data-after-beta-20260826-132255.tar.gz`（`47,196,112 B`，SHA-256 `cd540221018269085ddee1f5709bac21ca27f116fb8bc78940e10309173756c0`），含 11 个文件、6 条评论。首次备份命中了 systemd 的数据目录软链接，只得到 `119 B` 的链接条目，已改名为 `.invalid-symlink-only.tar.gz` 防止误用；随后按真实目录重新生成并校验完整备份。
- 首次 Beta 前端切换因发布包缺少 `/beta/` 目录层被健康闸门拒绝，旧前端持续服务；修正目录层后才切换。没有发生错误页面切流或数据丢失，旧 Beta 前端 `20260826-124647`、旧后端 `20260826-120610` 均保留为代码回滚点。

## 2026-08-26 Beta 文件版本（已发布）

- GitHub 已创建并推送 `beta` 分支；功能、视觉、安全头与单版本评论修复提交依次为 `551c4f2`、`b3da53e`、`6fc2d22`、`86f57c8`。正式功能分支与正式前端静态目录未切换。
- Beta 支持原短码追加 `.riv` 版本、默认打开最新版本、文件名下拉切换历史版本、精确上传日期，以及多版本评论分组；只有一个版本时直接展示普通评论列表，不显示版本标题。旧版本评论默认折叠，点击标题同步切换预览。上传按钮显示“上传新版本”，上传/下载图标为白色，质量/缩放/引擎选中态与时间轴同为低亮黄色。
- 干净提交通过 H5 lint、TypeScript、`/beta/` 生产构建、`45/45` 测试和 `npm audit = 0`；服务端 `11/11` 通过。隔离浏览器用 Guide / Question 两份真实 `.riv` 验证两版本默认、下拉切换和新旧评论分组；公网 `vGd` 验证单版本标题和分组均为 `0`，两条原评论直接平铺，页面日志无错误。
- Beta 前端已原子发布到 `/var/www/rive-host-beta/releases/20260826-124647`；兼容后端仍为 `/opt/rive-host/releases/20260826-120610`，正式前端继续指向 `/var/www/rive-host/releases/20260826-111929`。`/beta/`、`/beta/vGd` 均为 `200`，线上入口 JS 与干净构建 SHA-256 一致。
- 旧状态自动迁移为每个分享一个“版本 1”；迁移前后 9 个文件、6 条评论的旧字段逐项一致，`.riv` 仍为 9 份。完整回滚备份为 `/var/backups/rive-host-data-before-beta-20260826-120610.tar.gz`（`47,054,667 B`，SHA-256 `a218042b1f8da5517f898780d3eae17324a06fa71bc22cc13a2269320eebcb6e`），内含 9 份 `.riv` 与迁移前 `state.json`。
- 首次启动因发布包缺少 `server/` 目录层级被 systemd 拒绝，健康闸门阻止了 Nginx 切换；补齐目录并解除启动频率限制后 API 恢复，再加载 Beta 路由。无文件或评论丢失；当前 API PID 为 `248806`。

## 2026-08-26 JOJO 评论身份、黄色光标与时间轴按钮（已正式发布）

- 评论框 Hover 显示主题黄闪动光标；聚焦后假光标退出，真实 caret 保持黄色。聚焦时框上方显示已分配头像和昵称，头像可改为本机图片并压缩成 `64×64 WebP`，昵称限 `12` 个 Unicode 字符；原图不持久化。
- 新增 `GET /api/v1/comment-identity`，评论 POST 可携带经校验的本机昵称和 WebP 头像快照；旧评论和默认动物身份完全兼容。时间轴只在确实存在重复下划线前缀时显示“展开 / 整理”；无可整理分组时整组隐藏。
- 功能提交 `5855e33` 已推送。H5 lint、TypeScript、根路径生产构建和 `43/43` 测试通过，`npm audit = 0`；服务端 `10/10` 通过。隔离浏览器实测昵称截断、`64×64 WebP` 压缩和黄色光标，没有误发评论。
- JOJO 前后端均已原子切换到 `20260826-104757`；直接回滚点为前端 `20260826-084827` / 后端 `20260824-162054`。状态切换前后 SHA-256 一致，现有 `6` 条评论未变；完整数据备份为 `/var/backups/rive-host-data-before-20260826-104757.tar.gz`（`47,057,090 B`，9 份 `.riv`），独立状态备份为 `/var/backups/rive-host-state-before-20260826-104757.json`。
- 线上 `vGd` 实测：2 条时间轴无可分组前缀，展开/整理按钮为 `0`；评论框聚焦后显示“苔藓鼹鼠”和头像，黄色光标状态正确，WebGL2 仍默认选中，控制台无错误或警告。

## 2026-08-26 JOJO 文件行整块打开（已正式发布）

- 首页 `.file-open` 与详情侧栏 `.preview-file-row-open` 现在各自是覆盖完整文件主区域的单一按钮；封面、文件名、空白和云状态图标都统一执行选中并打开文件，文件菜单与上传失败重试仍保持独立操作。
- 云状态由可点击的 `FileUploadStatusButton / .file-sync-toggle` 改为纯展示的 `FileUploadStatus / .file-sync-status`，不再点击展开“云端已就绪 / 只保存在当前浏览器”反馈；上传中和失败详情仍自动显示。
- 发布隔离：分支已有的普通 H5 默认高清继续保留，但 JOJO 托管版通过 `DEFAULT_HOSTED_RENDER_QUALITY = 1` 维持线上平衡档，不把无关质量默认值变化带入本次发布。
- H5 lint、TypeScript、根路径生产构建和 `41/41` 测试通过，`npm audit` 为 0。应用内浏览器通过 SSH 只读转发正式 API，在 `http://127.0.0.1:4176/` 实测：首页点击云图标打开 `vGd`，详情侧栏点击云图标切到 `O1H`，两处旧 `.file-sync-toggle` 均为 0，控制台无错误和警告。
- 功能随前端 `20260826-104757` 发布；首页和详情侧栏都已统一为整行打开，云图标不再产生独立点击反馈。

## 2026-08-26 JOJO 评论提交与页内加载修复（已正式发布）

- “发表评论”按钮无法点击的原因是 `.comment-editor` 的层级覆盖了按钮区域；`.comment-submit-row` 现提升到 `z-index: 2`，按钮整块可点。普通 Enter 通过原表单 `requestSubmit()` 发表，Shift / Command + Enter 换行，IME 组字的 Enter 不误发。
- 侧栏切换云端文件改为 `history.pushState + React 路由状态`，不再使用 `location.reload()`。旧播放先暂停，详情布局保持；目标文件元数据返回后，文件名先更新，下载和解析进度只在预览画布内显示。
- 功能提交 `5757082` 已推送，干净工作树通过 H5 lint、TypeScript、根路径生产构建和 `40/40` 测试，`npm audit` 为 0。正式前端已原子切换到 `/var/www/rive-host/releases/20260826-084827`，直接回滚点为 `20260825-225304`；入口 JS、CSS 与干净构建逐字节一致，WebGL2、Canvas2D 与两份版本化 WASM 均保留。
- 正式 `O1H` 已验收：输入框可编辑，`Command / Shift + Enter` 都只换行；切换到 `vGd` 后 URL 页内更新、详情面板持续存在、没有全屏 Loading，浏览器无错误和警告。Nginx 未重载；后端代码仍为 `20260824-162054`，只因清理验收数据重启，最终 PID 为 `199462`。
- 评论排查确认没有丢失：`vGd / 点击反馈.riv` 保留原有“不错不错 / 牛逼”2 条，已归档 `mIY / ai叫叫学伴.riv` 保留原有 2 条。验收工具误触产生的 `O1H` 测试评论和上一轮遗留的 `vGd` 测试评论均按 ID 精确删除；清理前状态分别备份为 `/var/backups/rive-host-state-before-test-cleanup-20260826-085058.json` 与 `...-085420.json`。最终为活跃文件 5 个/评论 2 条、归档文件 1 个/评论 2 条，`O1H` 为 0 条。

## 2026-08-26 小程序回滚 Canvas（已完成）

- 真机确认 WebGL2 试验包无法正常加载后，微信小程序已恢复 `<canvas type="2d">` 和完整 `@rive-app/canvas-advanced@2.39.1`；H5 与 JOJO 版继续默认 WebGL2，不受影响。
- 小程序重新保留 `canvas_advanced.js`、`rive.wasm.br` 与 `rive_fallback.wasm.br`，主 WASM 失败时仍可尝试兼容 WASM；不包含 WebGL2 小程序运行时。试验产物已移到 `dist/mini-webgl2-experiment-archive-20260825/`，不会进入上传包。
- `npm run check`、`npm run verify:interactions`、Guide / Question 解析与 12 份真实文件基准通过；上传源包 `1,666,466 B / 1,700,000 B`，依赖审计为 0。
- 微信开发者工具 RC `2.02.2607271`、基础库 `3.17.0` 普通编译通过；真实 `引导页动画750_1160.riv` 的画面、画板 `叫叫`、状态机 `jojo-machine`、时间轴与约 `21–23 FPS` 均可见。未上传版本、未提审。

## 2026-08-25 普通 H5 默认高清与 WebGL2（已正式发布）

- H5 渲染质量默认值由性能改为高清，并在载入 `.riv` 前写入播放器，避免首帧先按性能档初始化；性能、平衡、高清仍可随时手动切换。
- 默认值没有新增 CPU 型号或内存猜测。普通素材高清档最高约为 `60fps + DPR 2.0`；超过 `2 MiB`、画板不少于 `24`、默认画板动画不少于 `48` 或状态机不少于 `24` 时，现有保护仍自动限制在最高 `30fps + DPR 1.25`。
- 微信小程序继续默认性能，并保留 `riveQualityMode` 本地记忆，不受本次电脑端调整影响。
- H5 lint、TypeScript、生产构建与 `38/38` 测试通过，`npm audit` 为 `0`。普通 H5 已原子切换到 `/var/www/rive-viewer/releases/20260825-230508`，直接回滚点为 `20260818-093940`。
- 公网 HTML、入口 JS 和 CSS 与本地生产构建 SHA-256 一致；HTML 预加载 WebGL2 WASM，两份 WASM 均为 `application/wasm + gzip + immutable`。内置浏览器桌面与 `360×800` 首页正常、无控制台错误。本轮未上传小程序、未提审。

## 2026-08-25 JOJO WebGL2 默认渲染与兼容降级（已正式发布）

- H5 默认渲染器已由 Canvas2D 改为 `@rive-app/webgl2-advanced@2.39.1`，`@rive-app/canvas-advanced@2.39.1` 保留为兼容模式；两套 JS 继续动态拆包，WASM 分别为 `rive-webgl2-2.39.1.wasm` 和 `rive-2.39.1.wasm`，HTML 只预加载默认 WebGL2。
- 文件预览参数最后新增“渲染引擎”，支持“WebGL2 / 兼容模式”手动切换。切换时重建 Canvas 并重新载入当前文件，规避浏览器同一 Canvas 不能更换上下文类型的问题。
- WebGL2 初始化、逐帧运行或上下文失败时自动降级 Canvas2D，并弹出约 `4.2s` 的“WebGL2 不可用，已切换到兼容模式”Toast；初始化降级复用已经下载的 `.riv` 数据。
- 真实 `ai叫叫学伴_源文件_绿豆.riv / 1sf` 本地验收：WebGL2 恢复角色体块柔化光影，Canvas2D 保持硬边但可正常播放；双向手动切换成功。临时移走 WebGL2 WASM 注入初始化失败后，页面自动选中兼容模式、画面继续显示且 Toast 正确出现，随后资源已恢复。
- 自动验证：H5 lint、TypeScript、默认基址与 JOJO 根路径生产构建及两套 `31/31` 测试通过，`npm audit` 为 `0`；构建确认入口、播放器、WebGL2 和 Canvas2D 分为独立 JS chunk，两份版本化 WASM 均生成 Brotli/Gzip。
- 正式前端已发布为 `20260825-192225`，源码提交为 `07466ed` 与键盘分栏修复 `9a7cd58`；上一轮发布尝试 `20260825-191649` 和发布前稳定版 `20260824-163850` 均保留。后端仍为 `20260824-162054`，PID `675355` 未重启，生产文件与评论数据未修改。
- 公网 HTML、CSS、入口、播放器、WebGL2/Canvas2D 分包和双 WASM 与本地产物逐字节一致；两份 WASM 均返回 `application/wasm + gzip + immutable`。正式 `1sf` 在浏览器中默认选中 WebGL2，柔和体积光影可见；该文件自身仍会输出“未关联 View Model”的 Rive 提示，不影响渲染与交互。iPhone 与低端 Android 的长期稳定性仍需真机持续观察。

## 2026-08-25 JOJO 横屏双栏与评论紧凑化（已正式发布）

- 托管详情在 `1194px+` 切为左预览、右检查双栏；右栏默认 `390px`、最小 `360px`，中缝支持 Hover 提示、指针拖动和键盘调整。左侧画布默认占满浏览窗口高度，但下方 `32px` 手柄继续显示，首次拖动从当前满高尺寸接管并转为手动高度；`1193px` 及以下保持原有上下布局。
- 评论正文为 `14px`，头像独立在左列，昵称与正文在右列的间距缩至 `2px`。评论时间靠右，一周内显示相对时间，满一周后显示绝对日期。输入框仅保留一行默认高度，发表按钮收入框内，隐藏可见标题和字数。
- 评论框聚焦或已有正文时，点击下方任意时间轴会在当前光标位置插入不可拆分的下划线名称；一条评论可嵌入多个不同或重复时间轴。发布仍存为包含 `【时间轴：名称】` 标记的普通字符串，列表按任意位置解析多个标记；发布前后均不显示底层括号，Hover 为主题黄，点击标记只切换同名时间轴且不重复插入。
- 已上云图标改为灰色，文件状态图标统一缩小。“归档”改为无边框透明操作，鼠标悬停评论或键盘聚焦时显示，触屏始终可见。
- H5 lint、TypeScript、默认基址与 JOJO 根路径生产构建的两套 `33/33` 测试均通过，`npm audit` 为 `0`；入口、播放器、WebGL2 与 Canvas2D 继续独立拆包，两份版本化 WASM 均保留。
- 本地 `1366×900` 真实浏览器验收：默认画布 `676px`、手柄 `32px`、工作区 `756px`，拖动后画布改为 `656px` 且手柄仍可见。正式 `/1sf` 实测评论框可在正文中依次嵌入 `Phone` 与 `Ipad` 两个标记，WebGL2 仍为选中态；验收草稿已清空，未提交评论。
- 公网 `/`、`/healthz`、`/1sf` 为 `200`，`/s/1sf` 为 `308`，`/api` 与 `/samples` 为 `404`；正式页已留在应用内浏览器供杨总查看。
- 正式前端已原子切换为 `20260825-201557`，功能源码提交 `e870de3`；直接回滚点为 `20260825-192225`。发布期间生产 SSH 目标从旧 `VM-0-13` 切换到新 `VM-0-7 / 124.221.104.244`，同一已验收前端包已重新发布到当前真实主机。公网 HTML、CSS、入口、播放器、WebGL2/Canvas2D 分包和双 WASM 与本地产物逐字节一致。新主机后端仍为 `20260824-162054`、PID `19337`，未因本次前端发布重启；活跃文件 `5` 个/评论 `2` 条、归档文件 `1` 个/评论 `2` 条均未变化。

## 2026-08-25 JOJO 时间轴、详情控制与文件栏优化（已正式发布）

- 时间轴新增“展开 / 整理”。整理态把无一级前缀的时间轴排在最前，再按第一个下划线分组；组内标签显示后缀，插入评论时仍使用完整名称。组标题仅缩进 `8px`，超过 `10` 条时标签收紧；Hover 标签会高亮对应组标题，事件行、时间轴标题行与当前组标题按 `0 / 56 / 88px` 三层置顶且互不遮挡。
- 评论输入框内使用 Calligraph 轮换功能提示，不再占用时间轴标题区；点击时间轴仍按当前光标位置插入，可在同一条评论里嵌入多个完整时间轴名称。事件条数量缩小并固定在右侧，播放描述超长时截断。
- 完整模式画布保持 Rive 文件原始宽高比；桌面先把预览分到约 `800px`，再扩展最小 `360px` 的右栏，满高画布与原手柄同时保留。详情页标题改为“Rive 预览台”，桌面侧栏返回改为关闭叉号，最近文件加号改为普通深色按钮。
- 左侧最近文件栏默认 `184px`，可在 `160–360px` 间用指针或键盘调整；Hover 或聚焦文件名时在栏外显示完整名称。自定义向上展开的倍速菜单替代原生控件；暂停时同步挂起 Rive 内部音频，继续播放时恢复。
- 干净提交构建通过 lint、TypeScript、根路径生产构建、`36/36` 测试与 `npm audit = 0`；正式浏览器已验收可调文件栏、完整文件名浮层、三层置顶、组标题 Hover 高亮、Calligraph 占位、自定义倍速和 WebGL2 默认渲染。WebGL2、Canvas2D、两份版本化 WASM 与本地产物逐字节一致。
- 功能提交为 `27595f4`、`f16c24d`、`1575477`，前端已原子切换到 `20260825-215521`，直接回滚点为 `20260825-211519`。后端仍为 `20260824-162054`、PID `19337`，没有重启；活跃文件 `5` 个/评论 `2` 条、归档文件 `1` 个/评论 `2` 条均未变化。工作区中未完成的默认高质量与小程序改动未进入本次发布。

## 2026-08-25 JOJO 时间轴数量默认布局（已正式发布）

- 时间轴数量超过 `10` 条时首次默认进入“整理”；恰好 `10` 条及以下仍默认“展开”。用户在当前文件手动切换后不会被播放进度或当前时间轴变化重置，切换文件或时间轴集合变化时重新按数量选择默认模式。
- `getDefaultTimelineLayout` 已覆盖 `0 / 10 / 11` 三个边界；干净提交构建通过 H5 lint、TypeScript、根路径生产构建、`37/37` 测试及 `npm audit = 0`。
- 功能提交 `52d117d` 已推送，正式前端原子切换为 `20260825-225304`，直接回滚点为 `20260825-215521`。正式 `O1H` 实测 103 条时间轴时“整理”选中、“展开”未选中，入口为 `index-Bn3efaQL.js`；双引擎分包和双 WASM 保留。后端仍为 `20260824-162054`、PID `19337`，发布前后活跃文件 `5` 个/评论 `2` 条、归档文件 `1` 个/评论 `2` 条不变。

## 2026-08-25 JOJO 内阴影与虚化只读诊断

- 问题存在。正式 JOJO 页的 `ai叫叫学伴.riv / mIY` 与 `ai叫叫学伴_源文件_绿豆.riv / 1sf` 都能正常下载、解析、播放和驱动状态机，但角色体块内的柔和阴影、虚化边缘与光晕呈硬边平涂；切到“高清”仍不恢复。
- 当前 H5 播放器使用 `@rive-app/canvas-advanced@2.39.1`，对应 Canvas2D 渲染器。用同一绿豆文件、同一个 `2.39.1` 版本、同一静态初始帧做本机 A/B：Canvas2D 丢失柔化光影，WebGL2 / Rive Renderer 正常显示，已排除文件损坏、下载失败和清晰度档位。
- `ai叫叫学伴_源文件_绿豆.riv` 另有“画板未关联 View Model”提示，但它与本次视觉差异无关：相同文件未改 View Model，仅切换渲染器即可恢复效果。
- 本轮只诊断并更新文档，没有改播放器代码、没有上传文件、没有发布或提交。建议下一步在隔离分支试切 `@rive-app/webgl2-advanced`，再覆盖大文件 FPS、WebGL 上下文、手机兼容性、WASM 预压缩与线上字节验收。

## 2026-08-25 黑客松包装文案与宣传封面

- 已基于真实公开页 <https://rive.mikeywa.site/vGd> 核对桌面 `1440×1000` 和手机 `390×844` 页面：绿豆角色、黄色复制链接、播放控制、事件条、FPS 和评论区均进入包装依据。
- `HACKATHON-2026.MD` 已整理作品定位、一句话介绍、6 个大众卖点、三档报名简介、评委话术、30 秒路演、演示顺序和拉票文案。核心叙事为“动效不该被压扁成录屏；发一个链接，让所有人看到同一个真实版本”。
- 最终 6 张宣传封面位于 `h5/assets/hackathon/posters/`，统一为 `1536×1024`、`3:2`。每张已生成居中 `86%` 安全区检查图，并缩到 `390×260` 复核标题与主要短句可读。
- `02-dingtalk-link.png` 使用真实短链 `rive.mikeywa.site/vGd`；所有封面使用真实绿豆角色和页面视觉，不再采用通用人物或虚构 Rive 面板。
- 边界：当前仓库没有钉钉原生登录、机器人或微应用代码，对外只称“适合在钉钉会话中分享的 H5 动效评审工具”。本轮没有改功能、没有部署、没有提交或推送。

## 2026-08-24 评论头像圆形展示

- 评论昵称前的头像展示层改为 `32×32px`、`border-radius: 50%` 与 `object-fit: cover`，源 WebP 继续保留完整方形底图，圆形只由 CSS 稳定裁切，不重复生成资源。
- H5 样式契约新增圆角与裁切方式检查，避免以后退回方角或 `contain` 留边。H5 lint、类型检查、根路径正式构建和 `23/23` 测试通过，`npm audit` 为 0。
- 纯前端正式版本已发布为 `20260824-163850`，前端 `current` 指向 `/var/www/rive-host/releases/20260824-163850`，上一前端 `20260824-162054` 保留回滚。后端仍为 `20260824-162054`，未重启服务、未改托管数据、未重载 Nginx。
- 公网首页与健康检查均为 `200`；正式 CSS `assets/index-B6HIOpdW.css` 与本地产物 SHA-256 一致，公开样式已确认包含 `.comment-author img{object-fit:cover;border-radius:50%;...}`。CSS 使用新哈希地址，浏览器无需清理旧头像缓存即可应用圆形裁切。

## 2026-08-24 评论按文件隔离与方形头像重制

- 评论线程现在由 `CommentThreadState.code` 明确绑定当前三位短码。切换文件会先清空旧线程，异步读取、发表、归档和恢复都只允许写回发起操作时的 `targetCode`；详情侧栏遇到已有云端短码时优先打开对应公开文件，不再用本地副本承接上一文件的评论状态。
- 服务端取消按 SHA-256 复用活跃短码。每次上传都会生成新短码、存储记录和独立评论区，即使上传内容完全相同；SHA-256 继续用于完整性、ETag 与示例识别。既有短码和评论数据不迁移、不合并。
- 萌萌森林头像母版重制为 `3840×1920` 的 `8×4` 方格，每格严格 `480×480px`。32 张运行时 WebP 均为不透明 `32×32px` 正方形，使用各不相同的满版底色、大动物轮廓和极简蜡笔线条；`h5/scripts/build-forest-avatars.py` 按比例格线逐格缩放，避免非方形裁剪、透明边缘细缝和二次居中裁偏。
- 图片检查：母版为 RGB PNG、无 Alpha；32 张头像全部通过正方形、`32×32px`、无 Alpha 检查，并以真实 32px 最近邻放大检查，小尺寸下颜色与动物轮廓清楚、格边完整。
- 自动验证：H5 lint、类型检查、根路径正式构建和 `23/23` 测试通过，`npm audit` 为 0；服务端 `10/10` 测试通过。真实浏览器使用 `guide.riv / UFb` 与 `question.riv / HFH` 两个独立评论：切换后旧评论命中数为 0、目标评论命中数为 1，头像自然尺寸与显示尺寸均为 `32×32px`，控制台无 warning/error。
- 正式前后端已发布版本 `20260824-162054`：前端 `current` 指向 `/var/www/rive-host/releases/20260824-162054`，后端 `current` 指向 `/opt/rive-host/releases/20260824-162054`；上一版本 `20260824-155022` 保留为回滚点。只重启 `rive-host.service`，新 PID 为 `675355`，Nginx 未重载且保持 `active`。
- 切换前完整数据备份为 `/var/backups/rive-host-data-before-20260824-162054.tar.gz`，SHA-256 为 `6679ec3301f87ff240df74075b768e2ab3fd264e85ad94cd1944c20e73084328`。发布前后均为 4 个分享、2 个活跃文件、2 个归档文件、3 条评论，未写入线上测试文件或评论。
- 公网验收：`/`、`/healthz`、`/RFS` 均为 `200`，健康响应为 `{"ok":true}`；首页、应用 JS 和“困困狐狸”头像与本地产物逐字节一致。线上 `vGd` 只返回自己的 2 条评论，`RFS` 评论列表为空。应用内浏览器在正式页读取时受当前网络影响超时，因此线上视觉结论只记为静态资源与公开 API 验收；完整交互已由同产物本地真实浏览器验证。
- 本地测试服务已停止；临时测试数据和 32px 检查图已移入废纸篓，可恢复。

## 2026-08-24 萌萌森林评论身份与评论归档

- 新评论不再显示“匿名”，也不要求用户输入名字。首次评论时浏览器生成随机访问标识，服务端稳定映射到 32 组动物昵称和头像；同一浏览器连续评论和刷新后评论都保持不变。无法保存浏览器标识时才用 IP 与 User-Agent 的即时哈希兜底，不保存原始 IP；清除站点数据、换浏览器或使用无痕窗口仍可能得到新身份。
- 32 个昵称与头像一一对应；头像视觉与裁切规格已由上方“评论按文件隔离与方形头像重制”替代。当前母版为 `3840×1920`，运行时共 32 张不透明方形 WebP，每张严格 `32×32px`。
- 评论昵称前显示 `32×32px` 头像。任何访问者都能归档活跃评论；归档评论在原顺序位置默认折叠，展开后显示原文和恢复按钮。服务端归档与恢复接口幂等，不硬删除评论，旧数据启动时补齐头像、状态和归档时间。
- 本地真实浏览器已验证同一用户连续两次评论、刷新后第三次评论均为“围巾小企鹅”及同一头像；归档后默认折叠、展开后恢复成功。`390×844` 与 `320×700` 均无横向溢出，头像自然尺寸和显示尺寸都是 `32×32px`，移动端输入框仍为 `16px`，浏览器无错误或警告。
- 本地验收服务已停止，三条临时评论及测试文件所在目录已移入废纸篓 `rive-host-ui-cWxtPL`，可恢复；正式环境没有写入测试评论。
- 自动验证：服务端 `10/10` 测试通过；H5 `23/23` 测试、lint、类型检查和正式根路径构建通过。新增覆盖稳定访问标识、32 组昵称/头像唯一性、评论归档/恢复、旧评论迁移和 32 张静态资源。
- 正式前后端已发布版本 `20260824-155022`。前端 `current` 指向 `/var/www/rive-host/releases/20260824-155022`，上一前端 `/var/www/rive-host/releases/20260824-121648` 保留；后端 `current` 指向 `/opt/rive-host/releases/20260824-155022`，上一后端 `/opt/rive-host/releases/20260822-233742` 保留。只重启 `rive-host.service`，新 PID 为 `665920`，Nginx 未重载。
- 切换前完整数据备份为 `/var/backups/rive-host-data-before-20260824-155022.tar.gz`，SHA-256 为 `81856e189f79c8eb69183ad8c4dfc1414450866067e96fe3296e5719ad30c605`。线上 4 个分享、3 条旧评论保持不变；3 条旧评论均自动补齐头像、活跃状态和归档时间字段，公开文件 `vGd` 的 2 条评论已从公网确认身份与状态完整。
- 公网验收：`/`、`/healthz`、`/RFS` 为 `200`，旧 `/s/RFS` 为 `308`，`/api` 与 `/samples` 为 `404`；`RFS` 前四字节仍为 `RIVE`。正式 CSS 和“围巾小企鹅”头像与本地产物 SHA-256 一致，服务与 Nginx 均为 `active`，发布后服务日志无 warning/error。浏览器能打开正式详情页，但当前网络下自动读取元素状态超时，因此视觉结论以同产物本地真实浏览器、线上静态字节和样式契约共同验收，不夸大为正式页完整交互验收。

## 2026-08-24 侧栏黑色加号与 GitHub 小程序码

- JOJO 详情侧栏黄色上传按钮内的加号固定为 `#0b0f14` 近黑色，不再继承灰色。新增 H5 样式契约测试，确保后续改动不会把正常态图标退回灰色；禁用态仍沿用全局透明度反馈。
- GitHub README 在版本表后直接展示 `240px` 宽的“Rive 预览台”小程序码，原图为 `h5/public/rive-viewer/mini-program-code.webp`，尺寸 `320×320`、体积 `32,008 B`。
- 为避免带入当前工作树的未提交功能改动，小程序码使用独立临时 worktree 提交；提交前已执行 `git pull --ff-only`，GitHub 默认分支 `main` 已推送 `9fb8c8d docs: add mini program code`。

## 2026-08-24 GitHub README 版本介绍

- README 首页新增“版本说明”，清晰区分微信小程序、H5 本地版和 JOJO（叫叫）托管版，并直接展示两个 H5 正式入口。
- H5 本地版说明为浏览器本地保存、不上传服务器；JOJO 版说明为公司内部交付和公开链接协作，支持上传、三位短链接、评论、归档与恢复，无需账号。
- 补充公开链接可继续转发、不能视为私密凭证的边界；该 GitHub 提交只调整 README，不带入当前工作树的功能代码，也不触发线上部署。
- GitHub 默认分支 `main` 已提交并推送 `cc8c9c7 docs: introduce H5 and JOJO editions`；为避免带入当前工作树的大量未提交功能改动，使用独立临时 worktree 只提交 README。

## 2026-08-24 评论功能恢复与匿名单输入

- 评论能力没有被删除，原入口位于右侧参数区最底部，并且评论读取要等待整份公开文件下载完成，因此在慢网和小屏中看起来像“评论不在了”。现在唯一的 `ShareCommentsPanel` 紧跟第一栏事件控制台，位于画板等参数之前；重复的公开链接卡已移除，详情右上角仍是唯一复制入口。
- 评论表单只保留一个正文输入区，不再显示、读取或保存称呼。占位文字为“写下评论或备注，可在内容里留下你的名字”；前端固定提交空昵称，服务端沿用现有规则显示“匿名”，已有带昵称的历史评论不受影响。
- 评论列表在公开元数据确认后与 `.riv` 下载并行读取。列表读取错误与发表评论错误已拆开：读取失败不再同时显示空态，可单独“重新读取评论”；发表失败会保留正文，按钮原位变为“重新发表”。初次读取与发表重叠时按评论 ID 合并，避免 GET 覆盖刚发表的评论；提交锁同时使用状态和 Ref，阻止快速重复 POST。
- 移动端评论输入字号为 `16px`，避免 iOS 聚焦缩放；提交按钮最小高度 `44px`。占位文字和状态小字提升到 `var(--muted)`，Textarea 继承全局字体并使用 `2px` 主题黄键盘焦点环。
- 本地真实浏览器覆盖 `1120×900`、`390×844`、`320×700`：三档均无横向溢出，评论区只有一个 Textarea、没有姓名 Input，事件控制台后紧接评论入口；本地发表后显示“匿名”和成功状态，刷新后评论仍在，浏览器无错误或警告。临时评论只写入本地测试服务，测试服务已停止、临时目录已移入废纸篓；正式环境没有写入测试评论。
- 自动验证：H5 正式根路径 `23/23` 测试通过，lint、类型检查、生产构建通过，依赖审计为 `0`；服务端 `7/7` 测试通过，`git diff --check` 通过。新增测试覆盖空昵称 POST、单输入框、挂载顺序、错误互斥、手机字号和焦点样式契约。
- 正式前端已发布版本 `20260824-121648`，`current` 指向 `/var/www/rive-host/releases/20260824-121648`；上一前端版本 `/var/www/rive-host/releases/20260824-113519` 保留为即时回滚点。后端仍为 `/opt/rive-host/releases/20260822-233742`，API PID `69166` 未重启，Nginx 未重载，数据未修改。
- 公网验收：`/`、`/RFS`、`/healthz` 为 `200`，旧 `/s/RFS` 为 `308`，`/api` 与 `/samples` 为 `404`；`GET /api/v1/shares/RFS/comments` 返回 `200` 和原有空列表。线上 HTML、入口 JS、CSS、播放器分包与 WASM 均和本地发布产物逐字节一致。

## 2026-08-24 公网下载带宽只读诊断

- 已排除应用与主机主动限速：Nginx 生效配置没有 `limit_rate`、`limit_rate_after` 或 `proxy_limit_rate`；现有 `limit_req` 只约束 POST 写入，`limit_conn 20` 只限制并发数。Node 直接 `createReadStream → pipeline`，Linux `tc` 只有 `mq + fq_codel` 且无丢包、超限，防火墙没有 rate/police 规则。
- `RFS` 为 `17,590,968 B`。服务器内部 Node 完整读取约 `0.029s`，Nginx loopback 约 `0.091s`；CPU 96–98% 空闲、Node CPU 0%、无 IO 或内存瓶颈，因此不是机器性能不足。
- 公网强制直连长期约 `52–190 KB/s`，20 秒整包样本约 `122–130 KB/s`；SSH 不经代理传真实文件约 `187–215 KB/s`。代理健康时可到 `171–257 KB/s`，但偶发连接重置；绕过代理不会根治，持续低速主因不是代理。
- 连续 5 次 1 MiB 下载聚合约 `0.962 Mbps`，不同协议共同落在约 `1–2 Mbps`，高度符合腾讯云公网带宽规格或云出口 QoS。实例 metadata 的 `bandwidth-limit-egress` 是约 `1.57 Gbps` 的内网限额，不代表购买的公网带宽。
- 精确公网套餐值只能在腾讯云控制台查看，或使用只读云 API 查询 `InternetAccessible.InternetMaxBandwidthOut`。Safari 当前腾讯云会话已退出，未登录、未修改配置、未产生付费变更。
- 理论上 `17,590,968 B` 在 1 Mbps 下至少约 `141s`，3 Mbps 约 `47s`，5 Mbps 约 `28s`，10 Mbps 约 `14s`，尚未计入 TLS、路由抖动与 Rive 解析。若需要明显提速，应提高公网带宽或把 `.riv` 本体迁到腾讯云 COS/CDN；调 Node、Nginx、gzip 或仅绕过代理都不能解决当前上限。

## 2026-08-24 公开文件冷启动修复与 JOJO 默认完整模式

- 另一个浏览器首次打开公开文件时，不再停在没有变化的“正在打开公开文件”。公开文件改为流式读取并显示真实下载百分比、文件名、体积和当前阶段；20 秒收不到响应或连续 30 秒没有新数据会明确失败并提供重试，用户主动离开仍会立即取消。
- 首帧不再等待 IndexedDB 最近记录或文件列表刷新：文件下载并建立播放器对象后立即进入预览，最近记录与云端列表改为后台补写。服务端已声明的文件体积会参与完整性校验，网络中断造成的残缺响应不会被当成成功文件。
- 根因不是服务器 CPU、内存或磁盘不足。服务器内网完整文件响应约 `0.06–0.12s`，Nginx 与 API 无 `5xx/429`；问题集中在约 `17 MB` 文件首次经过公网/代理下载时速度波动，旧界面又固定停留在假进度并可能被本地持久化阻塞，因而表现得像永久打不开。
- JOJO 托管版默认画布模式改为“完整”，首次打开即使用 `contain`；旧本地预览入口继续保持原有默认值。首页“已归档”数量改为透明背景、无圆角的普通灰色数字；详情复制成功后的 `check.svg + 已复制` 使用普通深色按钮，不再黄色高亮。
- 本地真实浏览器已用受控慢速流确认进度由 `17%` 持续到 `54%`；真实 Rive 文件确认默认选中“完整”，画布为等比完整显示；复制成功按钮计算样式为普通深色，归档数量为透明背景、`11px` 灰字。正式域名浏览器验收再次受当前代理下载超时影响中断，但线上静态字节、接口、路由、Range/ETag/缓存响应头均独立通过。
- 自动验证：H5 默认基址和正式根路径均为 `22/22` 测试通过，依赖审计为 `0`，`git diff --check` 通过。新增测试覆盖真实流式进度、响应不足、超时、首帧先于后台持久化以及三项样式契约。
- 正式前端已发布版本 `20260824-113519`，`current` 指向 `/var/www/rive-host/releases/20260824-113519`；上一前端版本 `/var/www/rive-host/releases/20260824-105538` 保留为即时回滚点。后端仍为 `/opt/rive-host/releases/20260822-233742`，API PID `69166` 未重启，Nginx 未重载，数据未修改。
- 公网验收：`/`、`/RFS`、`/healthz` 为 `200`，旧 `/s/RFS` 为 `308`，`/api` 为 JSON `404`，`/samples` 为 `404`；线上 HTML、入口 JS、CSS、播放器分包和 WASM 与本地产物逐字节一致，公开文件支持正确的 `206 Range`、`Content-Range`、ETag 和 immutable 缓存。

## 2026-08-24 叫叫版署名、直接复制与归档弹窗

- 托管 H5 正式称“JOJO 版”或“叫叫版”。`Brand` 统一在标题旁显示 `9px` 的 `for JOJO`；详情 `380px` 以下隐藏长标题但保留品牌图标与署名。首页底部的交互式作者入口已改为纯文本“反馈意见：杨皓棱”，不再复制微信号。
- 托管详情右上角只保留下载和黄色“复制分享链接”。手机与桌面共用 `copyActiveHostedLink`；成功后按钮原位变为 `check.svg + 已复制`，约 `2.2s` 后复位，失败原位显示“复制失败”。详情不再挂载分享弹层或快捷键入口；上传完成结果仍可使用 `ShareActionsDialog`。
- 首页不再直接铺开归档文件。最近文件标题右侧新增紧凑“已归档 + 数量”按钮，打开 `ArchivedLibraryDialog` 后才显示刷新、复制、恢复和空态；弹窗支持 Esc、遮罩空白、关闭图标、Tab 焦点限制和关闭后焦点返回。归档数据仍参与统一列表去重，归档与恢复 API 链路未变。
- 本地真实浏览器覆盖 `1120×900`、`390×844`、`320×700`：三种宽度无横向滚动，`for JOJO` 字号为 `9px`；桌面和手机详情文件操作严格为下载与复制两项，下载图标在手机可见；复制后显示单独打钩与“已复制”，系统剪贴板严格等于当前三位链接；归档弹窗在 320px 不溢出，恢复后行与数量即时更新，Esc 关闭并返回入口焦点。浏览器控制台无错误或警告。
- 自动验证：小程序 `npm run check` 通过，源包 `1,658,042 B`；H5 默认基址与正式根路径均为 `21/21` 测试通过，依赖审计为 `0`；服务端 `7/7` 测试通过。
- 正式前端已发布版本 `20260824-105538`，`current` 指向 `/var/www/rive-host/releases/20260824-105538`；上一前端版本 `20260824-084855` 保留为即时回滚点。后端仍指向 `/opt/rive-host/releases/20260822-233742`，API PID `69166` 未重启，Nginx 配置通过且未重载，数据未修改。
- 公网验收：`/`、`/RFS`、`/healthz` 为 `200`，旧 `/s/RFS` 为 `308`，`/api` 为 JSON `404`，`/samples` 为 `404`；线上 HTML、入口 JS、CSS、播放器分包、WASM 和新增 `check.svg` 与本地产物逐字节一致。正式域名的浏览器自动化仍因代理加载超时中断，公网路由、接口、响应头、状态数据和静态字节已独立验证；相同构建的完整交互已在本地真实浏览器通过。
- 当前工作树保留用户原有未提交改动，本轮未执行 commit 或 push；如需提交，必须先 `git pull`。

## 2026-08-24 详情侧栏稳定顺序与分享弹层

- 首页继续按最近打开、评论或上传排序；进入详情后，侧栏缩略图、标题、上一个/下一个都使用 `ActivityPolicy: preserve`，只切换黄色选中线，不置顶、不改 IndexedDB 活动时间，也不自动展开上传状态。纯云端文件跨三位路由时通过一次性 `history.state` 标记保持顺序，标记读取与清理分离以兼容 React StrictMode。
- 详情右上角保留下载，并在旁边新增带文字的黄色“分享”。手机和桌面共用 `ShareActionsDialog`：完整公开链接与右侧复制图标在上，全宽下载操作在下；Esc、遮罩空白和关闭图标都可关闭，Tab 焦点限制在弹层内并在关闭后回到分享按钮。
- 本地真实浏览器已覆盖 `1120×900`、`390×844` 和 `320×700`。侧栏在纯云端文件之间跨页切换并等待刷新后顺序不变；三种宽度都无横向滚动，320px 品牌文字自动收起但“分享”完整保留。复制内容与当前根路径公开地址严格一致，弹层下载会正常触发并关闭。
- 正式前端已发布版本 `20260824-084855`，`current` 指向 `/var/www/rive-host/releases/20260824-084855`；上一前端版本 `20260822-233742` 保留为回滚点。后端仍指向 `/opt/rive-host/releases/20260822-233742`，API 进程 PID `69166` 保持运行，Nginx 配置检查通过且无需重载，数据未修改。
- 公网验收：`/`、`/RFS`、`/healthz` 均为 `200`，旧 `/s/RFS` 为 `308` 跳转，`/api` 为 JSON `404`，`/samples` 为 `404`；线上 HTML、入口 JS、CSS、播放器分包和 WASM 与本地验收产物逐字节一致。线上 `RFS` 文件约 `17 MB`，经代理的远程浏览器自动化两次加载超时；交互验收以相同产物的本地真实浏览器结果为准，公网静态字节、接口、响应头和路由均已单独验证。
- 自动验证：小程序 `npm run check` 通过，源包 `1,658,042 B`；H5 默认基址与正式根路径均为 `21/21` 测试通过，依赖审计为 `0`；服务端 `7/7` 测试通过。

## 2026-08-23 托管详情常驻复制链接（已由 2026-08-24 分享弹层替代并发布）

- 托管文件打开后，手机顶部栏和桌面文件标题区都直接显示黄色“复制链接”，与下载同级，不再要求先打开文件菜单。
- 当前短码从公开文件、本地文件映射、上传完成状态和浏览器已记忆映射依次解析；刚上传完成的本地文件无需刷新即可复制新链接。旧 `/rive-viewer/` 本地模式不显示该按钮。
- 复制统一使用 `hostedShareUrl(activeHostedCode)` 生成根路径三位链接，并复用 `h5/lib/clipboard.ts` 的浏览器剪贴板与旧浏览器回退。成功显示“已复制”，失败显示“复制失败”，约 `2.2s` 后恢复。
- 本地真实浏览器已覆盖 `1120×900`、`390×844` 和 `320×700`：桌面按钮与下载同高 `42px`、间距 `8px`；两种手机宽度都无横向滚动，复制文字始终完整。点击后系统剪贴板与当前公开地址严格一致。
- 自动验证：小程序结构、手势与 `1,658,042 B` 包体检查通过；H5 旧基址与托管根路径均为 `21/21` 测试通过，依赖审计为 `0`；服务端 `7/7` 测试通过。
- 该版直接复制按钮未单独发布；2026-08-24 已改为“分享”弹层并随前端 `20260824-084855` 发布。工作树仍未提交或推送。

## 2026-08-22 托管平台体验更新

托管、评论、归档与恢复已经更新到 <https://rive.mikeywa.site/>。平台无用户系统，所有访问者权限相同；详情页也会保留整个平台的窄文件栏。

- 正式前后端版本为 `20260822-233742`。后端 `current` 指向 `/opt/rive-host/releases/20260822-233742`，前端指向 `/var/www/rive-host/releases/20260822-233742`；`rive-host.service` 与 Nginx 均为 `active`。
- 公开链接固定为大小写敏感的 3 位 Base62 根路径，例如 `https://rive.mikeywa.site/Ab1`；旧 `/s/Ab1` 会保留查询参数并以 `308` 跳转到 `/Ab1`，`api` 为保留码。
- 首页只保留一个“最近文件”列表，不再区分“托管文件”和“最近文件”。服务端公开文件、本机文件与浏览器最近查看文件按最近打开、评论或上传时间统一倒序，并按公开短码或本机映射去重。
- 文件体积后使用状态图标：电脑表示仅本机、云勾表示已上云、闪烁的上云箭头表示上传中、云叉表示失败。点击图标展开当前文件的行内状态栏；上传百分比、失败原因和重试都在同一条细进度栏中处理，不再显示独立完成卡片。
- 在首页选择或整页拖入 `.riv` 会保存并上传；详情页拖入文件或点击窄栏顶部加号时，会立即本地打开首个文件，同时在后台上传。旧入口 <https://mikeywa.site/rive-viewer/> 继续只保存在本机。
- 桌面详情页左侧为 `184px` 窄文件栏，展示统一文件列表、缩略图和云状态。当前项使用独立 `3px` 圆角黄色竖线，不使用黄色内阴影；手机窄屏隐藏该栏。公开评论放在右侧参数区底部。
- 控制区第一栏为事件控制台：折叠显示最新事件，展开显示当前文件会话最近 `200` 条绑定、播放、状态和 Rive Event。右上角快捷键图标支持桌面悬停、键盘聚焦和触屏点击；详情左上角与 `Esc` 都返回文件列表。
- 两份 H5 示例已归档并从活动列表、已归档列表及 `/samples` 静态路径隐藏：`question.riv` 为 `fep`，`guide.riv` 为 `sO0`。两份文件接口均返回 `410`，状态文件仍保留归档记录，可按原短码恢复，没有执行永久删除。
- 正式验收：`/` 与 `/healthz` 为 `200`，`/api` 为 JSON `404`，`/samples` 与两份示例静态地址为 `404`；HTML、入口 JS、CSS、播放器分包和 `rive-2.39.1.wasm` 均与本地发布产物一致，WASM 为 `application/wasm`。
- 本地真实浏览器覆盖桌面与 `390×844` 手机：统一列表、详情窄栏、黄色选中线、侧栏加号、详情拖入后立即打开并上传、公开详情评论、事件控制台和移动端自适应均正常。
- 自动验证：小程序 `npm run check` 通过，源包 `1,658,042 B`；H5 根路径与旧基址均为 `21/21` 测试通过，依赖审计 `0`；服务端 `7/7` 测试通过。
- 本次数据备份为 `/var/backups/rive-host-data-before-20260822-233742.tar.gz`，SHA-256 为 `829428ba4a28c677801b26dc858cedbcd494017aac80efbb6a8a30d98303ea46`；Nginx 备份为 `/etc/nginx/sites-available/rive.mikeywa.site.bak-20260822-233742`，上一版本 `20260822-154601` 保留为回滚点。
- 旧入口版本仍为 `20260818-093940`，不会丢失旧 Origin 的 IndexedDB 文件。当前工作树包含本轮和上一轮未提交改动，未执行 commit/push；如需提交，必须先执行 `git pull`。

## 本轮完成

整体性能、渲染、包体和 AI 可维护性重构已完成，并完成微信开发者工具打包、H5 本地与正式部署、真实文件验证。工作树尚未提交；小程序仍未正式上传或提审。

- H5 高频时间轴/FPS 使用 `PlaybackTelemetry + useSyncExternalStore` 局部更新；文件读取使用 session token，旧加载不会覆盖当前文件。
- H5 Rive 播放器动态拆包；入口 JS gzip `69.74 KB`，播放器 chunk gzip `29.39 KB`。正式构建链只保留 Vite，依赖审计为 `0 vulnerabilities`。
- 小程序手机详情和桌面分栏共用 `components/preview-panel`；组件查询固定 `query.in(this)`，WXSS 只用 `:host` 与类选择器。
- 双端性能阈值以 `shared/rive-policy.json` 为唯一真源，并通过 `npm run sync:policy` 生成微信 `.js` 模块。
- 小程序上传源包 `1,837,997 B → 1,631,271 B`，自动预算上限 `1,700,000 B`；微信开发者工具预览代码包显示 `1646 KB`。
- 微信开发者工具 RC `2.02.2607271`、基础库 `3.17.0` 普通编译成功；`引导页动画750_1160.riv` 真文件约 `21–23 FPS`，画板、状态机、时间轴与控制项正常。
- 微信开发版二维码已生成到 `dist/wechat-preview-2026-08-17/preview-qr.png`，并通过本地二维码检测与解码；本次有效至 `2026-08-17 23:01 +08:00`，过期后需重新生成。
- H5 本地生产预览曾运行在 `http://127.0.0.1:4174/rive-viewer/` 并完成验证；该临时服务当前不作为交付依赖。
- H5 正式入口 <https://mikeywa.site/rive-viewer/> 已部署版本 `20260818-093940`，上一版 `20260813-215427` 保留回滚。

## 真实文件验证

H5 已导入并验证六份本机真实 `.riv`：

| 文件 | 体积 | 结果 |
| --- | ---: | --- |
| `题目动画_1.riv` | 39 KB | 148×148、约 23 FPS、状态机正常 |
| `28334-53514-interactive-character-follow.riv` | 79 KB | 指针移动和点击正常 |
| `27375-51723-a-piano-game.riv` | 986 KB | 7 画板、Piano Game、点击与声音控件正常 |
| `27832-52591-animojis.riv` | 2.3 MB | 89 画板延迟展开成功 |
| `26133-49002-studiorun-a-cosmic-game-by-thelittlelabs.riv` | 12.7 MB | 17 画板、复杂状态机正常 |
| `25989-48561-room-decor-mini-game.riv` | 55 MB | 成功加载、画布正常、约 22 FPS |

大文件加载后 `150ms` 内切换回小文件，等待六秒仍稳定停留在小文件，约 `23 FPS`，无旧异步任务回写。个别素材没有给当前画板绑定 View Model，Rive 底层会输出兼容性提示，但未影响渲染与交互。

## H5 正式发布结果

- 发布目录：`/var/www/rive-viewer/releases/20260818-093940`；`current` 已原子切换。
- 回滚目录：`/var/www/rive-viewer/releases/20260813-215427`。
- Nginx 回滚配置：`/etc/nginx/snippets/rive-viewer.conf.bak-20260818-093940-retry`。
- 入口 HTML 返回 `200 + no-cache`；哈希 JS/CSS 和版本化 WASM 返回 Gzip 与一年 immutable 缓存，WASM 类型为 `application/wasm`。
- 线上 JS、CSS、WASM 解压后 SHA-256 与本地 `dist-static` 一致；内置 Guide 文件头为 `RIVE`。
- 第一次切换因本机验收未带域名命中默认站点而触发自动回滚；线上旧版本保持可用。改用绑定 `mikeywa.site` 的 HTTPS 验收后第二次切换成功。
- 内置浏览器已确认正式 URL 与页面标题可打开；受当前 VPS/代理下浏览器遥测超时影响，没有在正式域名重复整套交互。正式产物与本地已完成六份真实文件交互验证的构建逐字节一致。

## 已通过验证

```bash
npm run check
npm run verify:interactions
npm run benchmark:rive
cd h5 && npm run check && npm audit
```

结果：小程序结构、策略同步、6 项手势、包体预算、运行时模拟和 12 份真实文件基准全部通过；H5 lint、类型、生产构建、10 项测试和安全审计全部通过。Node Canvas 基准中的 `No WebGL support`、AudioEngine 和嵌入图片提示是无浏览器环境的预期回退。

## 已知边界

- 微信“编译提示 160”均为 `packOptions.ignore` 排除文件，不是编译错误。
- 微信代码质量面板建议主包小于 `1.5 M`，当前未达到；主要体积是 SIMD 与兼容版两份 Rive WASM。当前代码包可正常生成开发版二维码，并满足本项目 `1,700,000 B` 硬预算。
- H5 本地服务不是系统常驻服务，终端任务结束或电脑重启后需重新运行 `cd h5 && npm run preview -- --host 127.0.0.1 --port 4173`。
- 仍需 iPhone 真机确认 Piano 首次发声、快速连点、静音恢复与离页释放；开发者工具只能覆盖 Canvas 和交互基础链路。
- 若下一步提交，必须先按项目约定执行 `git pull`。后续每次正式发布继续核对 WASM 类型、压缩协商、immutable 缓存和 `current` 回滚点。
