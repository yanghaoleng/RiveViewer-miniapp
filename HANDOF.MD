# 当前交接

更新日期：2026-08-11

## 当前状态

- ✅ 原生微信小程序预览链路可用，完整 Rive Canvas 运行时固定为 `2.39.1`。
- ✅ 支持聊天文件批量导入、可用环境中的系统文件多选和本地文件库。
- ✅ 首页导入框下方的网页版入口和 `pages/web` 已移除；首页底部改为展示正式网址与复制图标，不再内嵌或自动跳转 H5。
- ✅ 底部已改为无分隔线单行“复杂文件用网页版更流畅：”；文字为 `24rpx`，整行左右各有 `20px` 内边距。界面省略网址的 `https://`，复制时仍写入完整网址；显示不下时以黑色渐隐遮罩截断，遮罩与最右侧复制按钮之间无缝隙。
- ✅ 底部“联系作者反馈意见”已统一为 `24rpx`；复制网址成功后明确提示“已复制网址”。
- ✅ 支持画板、状态机、时间轴、输入、倍速、画布背景和文件切换。
- ✅ 大文件分块读取、Loading 百分比、动态像素比和离页释放已完成。
- ✅ Piano Game 点击、View Model 绑定、Listener 优先级和 `in → idle` 调度已修复。
- ✅ Animojis 的 Rive Layout 与嵌套画板布局已恢复。
- ✅ 已定位体验版卡住的主要风险：嵌入图片失败不回调、Piano 内图片实际格式与声明不一致、旧 Canvas 绑定、SIMD 兼容和首页后台缩略图竞争。
- ✅ 已加入 WebP/PNG/JPEG/GIF 识别、图片失败回退、DOMMatrix 适配、双 WASM 回退、解析超时、复杂文件降帧和按大小限制像素比。
- ✅ 首页不再后台创建隐藏 Rive 画布；首次打开详情成功后才保存首帧封面，减少返回首页后的运行时竞争。
- ✅ 首页 `onReady` 后约 `600ms` 空闲预热 Rive WASM，点击文件会立即启动并复用同一 `runtimePromise`；不预解析具体 `.riv`。
- ✅ 小程序仍禁止 `wx.getClipboardData`；新增的 `wx.setClipboardData` 只在用户点击网址旁复制图标后写入固定地址，项目检查会阻止读取、后台写入或扩散到其他用途。
- ⚠️ 微信官方把 `wx.setClipboardData` 与 `wx.getClipboardData` 都归入“读取你的剪切板”隐私类目；下次提审前需重新声明，理由写明用户主动复制网页版预览地址，且不读取已有剪贴板内容。
- ✅ 已确认 Piano 文件内嵌 15 个声音资产：12 个 MP3 琴键音和 `switch / power01 / power02` 三个 FLAC 效果音。
- ✅ 已将 Rive WebAudio 链路桥接到 `wx.createWebAudioContext`；含音频文件默认开启，参数区可切换“开启 / 静音”并持久化选择。
- ✅ 静音、暂停和离页会暂停音频上下文；重新开启、播放、输入和画布点击会恢复，切换画板或时间轴后继续继承音量。
- ✅ 超过 8 MB 的 Rive 文件自动跳过音频解码并显示“性能保护”，避免大文件重新出现卡死；运行时录音入口已禁用，不申请麦克风。
- ✅ iOS Piano 现在拦截所有内嵌音频，12 个琴键音由 `BufferSource` 直接播放；实际 Piano 加载与点击测试后 `ScriptProcessor/miniaudio device` 均为 0。
- ✅ iOS 音频文件使用约 30fps、DPR 上限 1.5 和 100ms 状态更新节流；Piano 首帧后再准备声音，恢复期只保留最新音符，同时音源上限 16。
- ✅ 琴键优先解码、单资源 1.8s 超时继续、资源不完整或解码失败时显式静音；未知音效也不会回退到旧混音通道。
- ✅ 小程序包内 H5 预览、桥接、`web-view` 页面和浏览器入口资源均已移除；独立 H5 构建继续不进入小程序包。
- ✅ 许可证采用 `PolyForm-Noncommercial-1.0.0`，第三方 MIT 声明已补齐。
- ✅ GitHub 公开仓库已创建并推送：<https://github.com/yanghaoleng/RiveViewer-miniapp>。
- ✅ 微信开发者工具已在 2026-08-03 22:25 将最新性能构建覆盖上传为 `1.0.3`，更新类型“修订补丁”，备注“新增 Rive 声音播放与开关”。
- ⚠️ 杨总此前已删除“读取你的剪切板”隐私项；本次为实现一键复制网址，需要在下次提审前重新添加与实际用途一致的说明。
- ✅ 2026-08-07 再次全项目检查：小程序没有 `wx.getClipboardData`，只在用户点击复制按钮后以 `wx.setClipboardData` 写入固定网址。隐私用途建议填写：“用于用户主动复制网页版预览地址，方便在浏览器中打开复杂 Rive 文件；仅向剪切板写入固定网址，不读取剪切板已有内容。”
- ✅ 微信官方当前限制为单个主包/分包不超过 `2M`；开发者工具“主包小于 1.5 M”只是代码质量建议。当前主包约 `1.68 MiB`，符合上传和提审要求。
- ⚠️ 最近的网址显示、复制按钮与渐隐遮罩修改尚未重新上传；提审前需先补充剪切板用途说明并上传当前本地构建。
- 🔎 2026-08-11 已完成电脑端次级页导航静态排查，未操作微信账号：左侧导航使用 `align-items: flex-start`，返回点击层又以 `-11px` 上移；电脑端顶部窗口区域可能吞掉大部分命中范围。同时胶囊留白算法没有异常值与窗口变化兜底，会把标题挤歪。
- 🔎 返回方法无条件执行 `wx.navigateBack`；电脑微信直接恢复或打开预览页、页面栈只有 1 层时没有上一页可退，视觉上表现为按钮无响应。
- ✅ 2026-08-11 根据杨总提供的电脑端截图完成导航修复：Mac/Windows 依据胶囊 `top / height` 计算顶部补偿，异常坐标回退 `72px` 侧边留白，窗口缩放自动重算。
- ✅ 返回按钮改为完整 `44×44px` 命中区并取消纵向负边距；左侧与标题垂直居中。页面栈只有一层时使用 `wx.reLaunch` 返回首页。
- ✅ 已加入桌面截图参数、手机安全区和异常胶囊坐标回归检查；项目检查及 Rive 交互验证通过。电脑微信仍由杨总本人验证，不代操作微信。
- ❌ 2026-08-11 首页右缘和预览页左缘的 `2px` 页面遮罩经杨总 Mac 实测无效，已完整撤回，不保留无效样式和回归检查。
- ✅ 新截图确认白线属于微信 Mac 原生双 WebView 容器的分隔层；试验分支已改为同一页内的 `50% / 50%` 自定义分栏，不再进入原生双页容器。
- ✅ 原生详情页与嵌入式右侧面板共用 `preview-definition.js`，Rive 画布、状态机、音频、加载进度、文件切换和导出逻辑不分叉。
- ✅ 桌面端分栏阈值为 `960px`；缩窄时回到独立详情页，独立页拉宽后使用 `wx.reLaunch` 回流同页分栏。手机端不启用此逻辑。
- ✅ 改动前基线已提交为 `af94e59`，试验改动位于 `codex/mac-single-page-split`，可直接切回备份。
- ✅ 已准备重新打包 Mac 同页分栏试验版，交付位置为 `dist/RiveViewer-miniapp-1.0.4-mac-split-test.zip`；包内不含 H5、测试素材、`node_modules`、脚本和本地私有配置。
- ✅ 2026-08-11 已在开发者工具创建 `1200 × 800` 桌面宽屏、执行普通编译并打开同页分栏；放大与像素亮度检查均未发现浅灰中线，中心区域保持深色。对应重编译包为 `dist/RiveViewer-miniapp-1.0.4-mac-split-devtools-rebuild.zip`，SHA-256 为 `43a3779be175646aada9ea16962db2eb56aaaaa4eeb692c293dc409ce1728a1a`。
- 🔎 2026-08-11 完成同页分栏影响评估：普通 Mac Retina 窗口很可能低于 `960` 逻辑像素，实际仍走原生双页，因此当前常用窗口性能影响很小；真正启用同页模式后只有一个 Rive Player，但首页 `200% + scale(.5)` 可能增加合成开销，跨阈值会销毁并重新加载当前 Rive，窗口内缩放后还需主动同步 Canvas 尺寸与点击坐标。
- 🔎 2026-08-11 确认格式边界：`.riv` 是 Runtime 播放文件，`.rev` 是 Rive 编辑器备份/工程文件；小程序与 H5 当前均只导入 `.riv`，不计划直接解析 `.rev`。
- ⏳ 公众平台需要重新扫码登录后才能完成提审；二维码已打开，审核尚未正式提交。
- 🔎 已完成 Animojis 性能诊断：文件 `2.33 MiB`，含 `89` 个画板、`422` 条动画、`86` 个状态机；默认 `ANIMOJIS` 画板为 `3000 × 3000`，聚合大量嵌套表情。
- 🔎 此前首帧会同步全量 `inspectFile()`、重复创建默认复杂画板，并立即渲染 `89` 个画板标签；该启动热点已在本轮消除。
- ✅ Animojis 首帧热点已消除：只检查并复用默认画板，其余画板通过“展开其余 N 个”分批读取并显示进度，选中后才检查完整参数。
- ✅ 页面不再通过 `setData` 保存完整画板、状态机和动画对象；首帧只创建当前画板标签。
- ✅ iOS 复杂无音频文件采用约 `24fps`、DPR 上限 `1.25`；暂停取消 RAF，交互和参数变化时按需重绘。
- ✅ 已补懒画板目录、暂停绘制与复杂文件性能档检查；项目检查、交互验证、12 个 Rive 文件基准和开发者工具后台编译均通过。
- ✅ 最新 Animojis 懒加载、暂停按需绘制和复杂文件性能档已包含在本次 `1.0.4` 上传包中。
- ✅ 分享代码已核对：首页和预览页运行时都存在 `onShareAppMessage` / `onShareTimeline`，能力判断已修正为 `showShareMenu.object.menus`，项目检查覆盖两页。
- ✅ 正式分享素材已接入：首页和详情页好友卡使用 Figma 节点 `163:729` 的 5:4 标准 RGB PNG（500×400，约 123 KB），朋友圈使用圆形小程序图标（272×272，约 14 KB）；项目检查会验证路径和体积上限。
- ⚠️ 微信开发者工具 RC 已确认分享回调返回标题与 `/share-friend.png`，但当前 AppID 菜单仍同时提示“当前页面未设置分享”，模拟器分享对话框未绘出本地图片；需在平台分享权限恢复后用体验版真机确认最终卡片，不把该 RC 预览当作素材失效。
- ✅ 两份内置示例已允许从首页三项菜单删除；删除以 `rive-preview-hidden-builtins-v1` 本地记录持久隐藏，清除小程序数据后恢复，代码包内示例资源不被物理改写。
- ❗ 微信开发者工具对 AppID `wx529a71a5d297ac8b` 实测返回 `showShareMenu:fail no permission`；即使完全不调用 `wx.showShareMenu`、只保留页面回调，右上角仍显示“当前页面未设置分享”。根因是 AppID 平台权限，不是 `1.0.3` 页面代码或缓存。
- ✅ 已复核腾讯模型建议：临时加入 `enableShareAppMessage` / `enableShareTimeline` 后重新编译，菜单无变化；仅请求 `shareAppMessage` 也无变化。两字段属于 Taro 等框架用于保留分享生命周期的编译提示，不在微信原生页面/全局配置清单中，测试后已移除。
- ✅ 已在项目内新增独立 `h5/`，固定入口 `/rive-viewer`；支持批量导入、IndexedDB 本地文件库、画板/时间轴/状态机/输入、声音、倍速、背景、Fit 与三级渲染质量。
- ✅ H5 使用完整 `@rive-app/canvas-advanced@2.39.1`，提供加载百分比、首帧耗时、FPS、复杂画板懒加载、DPR 限制和离页释放。
- ✅ H5 预览详情已移除旧的 Rive 参数/画面参数一级 Tab；全部控件合并到同一纵向面板，手机端自动多行平铺。
- ✅ H5 Canvas2D 已改用 `renderer.align()` 绘制，完整/铺满真实生效；CSS 坐标先换算高 DPI backing canvas，再用同一矩阵映射到画板，Piano 点击和 Fit 不再错位。
- ✅ H5 用户点击会优先恢复所选状态机，覆盖默认 `in → idle → out` 串行流程；暂停、缩放、输入和点击均可请求单帧刷新。
- ✅ H5 已完成 ESLint、生产构建和 7 项服务端/资源/布局契约/坐标测试；微信 `project.config.json` 已排除 `h5/`，不会打进小程序上传包。
- ✅ H5 Sites 生产版本 3 已发布并设为公开：<https://rive-viewer-miniapp.muhualei.chatgpt.site/rive-viewer>；真实浏览器已验证同一首屏与两份内置示例，版本 3 部署状态成功。
- ✅ H5 已把 Next 运行依赖升级到 `16.3.0` 修复版；升级后生产构建、3 项测试和 `npm audit --omit=dev` 均通过，生产依赖 0 漏洞。
- ✅ 正式域名 `mikeywa.site` 指向腾讯云轻量服务器 `115.159.25.169`；`https://mikeywa.site/rive-viewer/` 已由独立 Nginx 路径上线，根首页仍为“羊石坨坨”。
- ✅ Nginx 仅新增 `/rive-viewer` 路由，HTML 不缓存、静态资源缓存 7 天；`rive.wasm` 返回 `application/wasm`。
- ✅ 已增加纯静态 Nginx 构建：`npm run build:static` 输出 `h5/dist-static/`，当前约 `2.5MB`，压缩发布包约 `857KB`；不需要在 2GB 服务器上常驻 Node 进程。
- ✅ 本机已生成专用 ED25519 密钥 `~/.ssh/mikeywa_rive_deploy`，SSH 别名为 `mikeywa-rive`，指纹 `SHA256:Yxz5DpJIfvzsk1BNZLVyImIvyWiQqOwKcAAuWfr1o40`。
- ✅ 已通过 OrcaTerm 连接 `OpenClaw(龙虾)-BpTX`，公钥已写入 `ubuntu` 的 `authorized_keys`；`ssh mikeywa-rive` 免浏览器登录验证成功，且具备免交互 `sudo`。
- ✅ H5 正式发布版本已更新为 `20260804-160847`，目录 `/var/www/rive-viewer/releases/20260804-160847/rive-viewer`，`current` 已原子切换；HTML、顶层快捷键浮窗、意见反馈复制、响应式 CSS、favicon WebP 和 WASM 均已复验。
- ✅ 本轮真实回归：Piano 启动键与琴键可点击，Animojis 总览网格和子画板布局正常，完整/铺满矩阵与画面同步变化，390 × 844 手机尺寸下画布和同页参数均可用。
- ✅ `1.0.4` 已加入小程序/H5 三级渲染质量，默认性能；复杂文件会自动限制 DPR 与帧率，高清选择不能绕过安全上限。
- ✅ 小程序与 H5 的实时 FPS 已统一移到播放时间右侧；播放工具栏下方只保留状态，不再显示首帧耗时。
- 🔎 Animojis 在 iOS 小程序的代码上限约为 `24 FPS + DPR 1.25`；真机只有约 `4 FPS` 说明单帧绘制本身超过 `200ms`，不是一个 `4 FPS` 平台开关。
- 🔎 微信小游戏具备更合适的 WebGL 持续渲染面，但 Rive WebGL2/WASM 需单独适配；建议先做 Animojis 单文件 POC，不直接整体迁移。
- ✅ H5 文件库将首帧压成 `160×120` WebP 存入 IndexedDB；实际验证 Animojis 缩略图可在列表和桌面侧栏立即复用。
- ✅ H5 首页已恢复原有纵向布局；跨过 `860px` 时，首页与右侧预览会用约 `260ms` 连续展开/收起，并兼容系统“减少动态效果”。
- ✅ 宽屏停留在 `860px` 及以上时，从首页打开文件也会连续收窄首页并展开预览；真实浏览器量测到 `860/0 → 713/147 → 442/418 → 400/460` 的中间列宽。
- ✅ 预览左上角已统一改为叉号；按钮点击和 `Esc` 均可关闭当前文件，真实浏览器已验证宽屏与窄屏行为。
- ✅ H5 已加入右上角快捷键浮窗；`R`、空格、四个方向键、`+/-` 与 `Esc` 均完成真实按键回归。
- ✅ 快捷键浮窗已提升到页头最高交互层；真实浏览器验证浮窗中心命中元素属于浮窗自身，画布不会盖住。
- ✅ H5 首页底部已加入“联系作者反馈意见”；点击写入微信号 `yanghaoeleng`，真实浏览器验证出现“微信号已复制”。
- ✅ H5 页头右上角的 `H5` 平台字样已移除，键盘快捷键入口保持原位。
- ✅ H5 与小程序均已去掉播放按钮下方的状态小字；H5 文件操作箭头默认隐藏，仅在悬停、聚焦、触屏或菜单展开时显示。
- ✅ 真实浏览器已验证 `1200px`、`860px` 和 `390×844`：桌面首页与右侧预览并存，无横向溢出；手机端仍为独立预览页。
- ✅ Figma 羊石节点 `152:723` 已导出为 WebP；favicon 增加 PNG 回退、WebP shortcut 和 `?v=2` 缓存刷新标记，并复用到页面左上角标题前。
- ✅ 微信开发者工具已于 2026-08-04 14:56 上传 `1.0.4`，更新类型“修订补丁”，备注“优化渲染性能与体验”；工具明确显示“代码上传成功”。

## 验证命令

```bash
npm ci
npm run vendor:rive
npm run check
npm run verify:interactions
npm run benchmark:rive
```

性能素材保存在本机 `rive测试文件/`，不进入公开仓库。`project.private.config.json`、`node_modules/` 和系统文件同样已忽略。

本轮已通过项目检查、Piano View Model 点击链、15 个音频资源捕获、真实 Piano 零 `ScriptProcessor/miniaudio`、延迟 resume 最新音符、16 音源上限、播放异常清理、Guide idle 调度、图片失败回退、无 DOMMatrix 环境和 12 个测试文件性能基准。2026-08-07 追加通过示例删除本地隐藏验证；微信开发者工具模拟器已打开首页，问题面板显示 0 个问题。本轮未上传新版本。

## 后续工作

- 登录微信公众平台检查通知中心是否有能力处罚/整改通知，并在接口权限或账号主体状态中确认分享能力；后台无可恢复项时，携带 AppID、线上版本 `1.0.4` 和 `showShareMenu:fail no permission` 完整错误提交平台申诉。代码无法绕过该权限拒绝。
- 用 iPhone 17 Pro 真机验证 Piano：首次打开时间、快速连点/滑键、开启 → 静音 → 开启、暂停/恢复、离页后不继续占用声音；模拟器不能代替真机音频调度确认。
- 当前 `1.0.4` 已上传；如需发布正式版，下一步在公众平台把该开发版本选为体验版并提交审核。
- 继续用手机体验版验证大文件返回、引导页点击优先级和 Animojis 布局。
- 文件选择用途如被平台再次询问，可写：“用于用户主动选择并导入 Rive 文件，在设备本地解析、预览和保存，不上传服务器。”
- 商业授权需由版权方单独书面许可，不能直接沿用公开仓库的非商用授权。
- Animojis 仍需 iPhone 真机复测；若 `24fps + DPR 1.25` 仍掉帧，再加入按帧耗时自适应到 `20/15fps + DPR 1.0`。
- 素材侧把总览改为静态缩略图或按表情拆分，仍是持续渲染的最大收益方案。
- 用 iPhone 打开正式 H5 入口，分别导入 Piano 与 Animojis，记录首帧耗时、稳定 FPS、首次点击延迟和声音连点表现，与小程序同文件同网络对照；桌面与自动化回归不能替代 iPhone 音频调度确认。
