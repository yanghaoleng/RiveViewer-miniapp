# RiveViewer Miniapp

一款在微信小程序和独立 H5 中预览、检查和交互调试 `.riv` 文件的工具。微信小程序与旧 H5 只在当前设备保存；新托管域名的上传入口会把用户主动选择或拖入的文件上传到云端并生成公开链接。

公开仓库：<https://github.com/yanghaoleng/RiveViewer-miniapp>

## 版本说明

| 版本 | 入口 | 文件保存 | 适合场景 |
| --- | --- | --- | --- |
| 微信小程序 | 微信内使用 | 仅保存在当前设备 | 手机端快速导入、预览和交互检查 |
| H5 本地版 | <https://mikeywa.site/rive-viewer/> | 仅保存在当前浏览器，不上传服务器 | 在桌面浏览器检查复杂 Rive 文件，保留素材本地性 |
| JOJO（叫叫）托管版 | <https://rive.mikeywa.site/> | 用户主动选择或拖入后上传云端 | 公司内部交付、公开链接分享、评论协作和归档管理 |

### 微信小程序码

<img src="./h5/public/rive-viewer/mini-program-code.webp" alt="Rive 预览台微信小程序码" width="240" />

H5 本地版与 JOJO 版共用 Rive Web 播放能力，支持画板、状态机、时间轴、输入、声音、事件控制台、缩放、背景、渲染质量和渲染引擎检查。两个正式入口均默认使用 WebGL2 / Rive Renderer，以呈现 Vector Feathering 制作的柔和内阴影、虚化边缘和光晕；初始化或运行失败时自动切到 Canvas2D 兼容模式并显示 Toast，也可在参数区最后一项手动切换。H5 本地版不生成分享链接；JOJO 版是在 H5 预览器基础上增加的轻量托管版本，标题带有 `for JOJO` 标识，文件上传后生成根路径三位短链接，任何拿到链接的人都可预览、下载、评论、归档或恢复文件，无需注册和登录。公开链接可以被继续转发，不应当作私密访问凭证。

微信平台当前上传并提审版本：`1.0.7`，更新类型：`修订补丁`，备注：`优化渲染性能与体验`。本版统一小程序/H5 的画布手柄、缩放快捷项和 Gentle 弹簧反馈；复杂文件继续自动限制 DPR 与帧率，实时 FPS 仅显示在播放时间右侧。

## 产品目标

- 让动效、设计和研发人员在手机上快速检查 Rive 文件。
- 在同一屏内查看画布、时间轴、状态机和输入反馈。
- 大文件加载期间保持页面可返回，并提供阶段与百分比反馈。
- 小程序与旧 H5 只在本地保存；新托管域名按文件展示仅本机、上传中、已上云或失败状态，成功后从文件菜单复制公开链接。
- 浏览器 H5 以 WebGL2 保证视觉还原；微信小程序使用原生 Canvas2D，以加载稳定性、交互和性能为准分别验收。

## 核心功能

- 从微信聊天文件批量导入，最多一次选择 100 个 `.riv` 文件。
- 支持可用环境中的系统文件多选；不支持时自动只展示聊天文件入口。
- 微信小程序内置两份示例文件，示例和已导入文件都可从本地列表删除；示例删除后以本地隐藏记录保持不再出现，清除小程序数据可恢复。
- 微信小程序首页的导入框、文件行、操作箭头、文件菜单、复制按钮和反馈入口都有即时放大回弹；小型按钮按下放大到 `1.25×`，大面积导入框与文件行使用 `1.025×` 避免遮挡。小程序文件操作菜单只保留“发送文件、删除文件”；H5 不提供不可恢复删除。
- 首页底部单行展示“复杂文件用网页版更流畅：”、`mikeywa.site/rive-viewer/` 和复制图标，整行左右各有 `20px` 内边距；空间不足时网址末端以黑色渐隐遮罩表示截断，遮罩与复制按钮无缝衔接。复制内容仍为完整的 `https://mikeywa.site/rive-viewer/`，成功后提示“已复制网址”。
- 底部“联系作者反馈意见”与网页版提示统一使用 `24rpx` 字号。
- 读取画板、状态机、时间轴和状态机输入；支持 Trigger、Boolean、Number 等交互。
- 支持完整/铺满、五种画布背景、播放暂停、重置、`0.5x / 1x / 1.5x / 2x / 8x` 倍速；单击画布手柄会在原位展开“完整 / 铺满”并在 `3s` 后自动收起，按住或拖动时手柄高亮并继续支持上下调高、横向滑选，`500ms` 内双击直接在完整与铺满之间切换。手柄默认较粗、展开后变细，左右项以淡黄色背景和同步变黄的 Phosphor Bold 图标/文字反馈，并使用约 `420ms` 的 Gentle 弹簧展开与反向收起。小程序在 `touchend` 内完成单/双击判定，不依赖可能因布局更新而丢失的后续合成 `tap`；手指轻微抖动的容差为 `16rpx`。
- 预览菜单所有可操作按钮按下即从中心放大到 `1.25×`，松开以弹簧曲线复位；播放速度和文件切换按住 `150ms` 即展开菜单，点菜单外空白区域可取消并收起。文件菜单支持不松手滑到任意文件，目标行即时高亮，松手直接打开。
- 画布下方的重播、播放/暂停、播放速度、上一个、下一个共五个按钮按五等分平等撑满整行，小程序独立详情、Mac 同页预览和 H5 保持一致。
- 小程序与 H5 的功能图标统一使用 Phosphor Icons `Bold`，由同一脚本固化成本地 SVG；H5 通过 CSS Mask 按需请求当前页面使用的图标，不再打包整套 React 图标库。
- 小程序五格工具栏把重播、播放/暂停、倍速、上一个、下一个改为五个非原生 `view role="button"` 一级项；使用 `width: 0; flex: 1 1 0` 严格等分，规避微信端原生 `button` 将百分比宽度按整行计算并覆盖翻页按钮。
- H5 时间轴标签在通用标签基础上左右各增加 `14px` 内边距；当前时间轴使用独立黄色半透明填充层显示实时播放进度，标签文字始终保持白色。播放时长优先从动画定义的工作区帧数与 FPS 计算，兼容运行时实例不暴露 duration 的文件；OneShot 结束强制填满，Loop 与 PingPong 分别回绕和往返。
- H5 控制区第一栏为事件控制台：折叠时显示当前文件最新事件，展开后显示本次打开期间最近 `200` 条绑定、播放、状态切换和 Rive Event；OpenUrl 只记录，不自动跳转。
- 参数顺序为状态机输入后紧接预览背景；H5 在缩放方式后以“渲染引擎”收尾，小程序仍以缩放方式收尾。背景色块使用 `60rpx × 40rpx` 易点按短胶囊，Loading 进度环保持正圆。
- 默认按 `in → idle → out` 组织常见时间轴；带 Listener 的状态机优先接管用户点击。
- 画布触摸事件直达 Rive 状态机；Piano Game 一类嵌套 Listener 与 View Model 文件可交互。
- 首页首屏稳定后预热 Rive WASM，点击文件时也会立即启动；不在首页预解析 `.riv`，避免大文件常驻内存。
- 大文件分块读取、分阶段 Loading、初始化前先刷新加载界面、按文件大小限制像素比、复杂文件约 30fps 渲染、加载超时重试和离页资源释放。
- Piano、Animojis 等嵌入资源较多的文件支持图片格式识别、图片失败回退、兼容版 WASM 和 DOMMatrix 适配。
- 含音频的 Rive 文件默认开启声音；参数区提供“开启 / 静音”开关并记住用户选择。
- iPhone 上的 Piano 会拦截全部内嵌音频，12 个琴键音使用 `WebAudio BufferSource` 直接播放，不再启动 Rive `ScriptProcessor/miniaudio`；恢复期连点只保留最新音符，最多同时保留 16 个音源。
- iOS 含音频文件使用约 30fps、像素比上限 `1.5`和状态反馈节流，减少动画、音频和视图更新争抢主线程。
- 超过 8 MB 的文件自动跳过音频解码并显示“性能保护”，避免声音资源使大文件加载和返回再次卡住。
- 上一个/下一个文件快速切换；长按可打开完整文件列表。
- Mac/Windows 与开发者工具在窗口宽度达到 `960px` 时，文件列表和 Rive 预览在同一个小程序页面内左右并排，不再触发微信 Mac 原生双 WebView 白色分隔线。
- `h5/` 提供独立浏览器版：预览详情不分 Tab，画板、状态机、时间轴、输入、声音、缩放、背景和渲染质量在同一页；入口固定为 `/rive-viewer`。

## 技术架构

| 模块 | 职责 |
| --- | --- |
| `pages/index` | 导入、本地文件库、文件操作与空闲 WASM 预热；不在后台解析文件或渲染缩略图 |
| `pages/preview` | 路由、分享和预览配置；全页与桌面分栏共用 `components/preview-panel` 模板 |
| `utils/library.js` | 本地文件、示例隐藏记录、元数据、封面缓存和分块读取 |
| `utils/rive-native.js` | 微信 Canvas2D 双 WASM 初始化与复用、资源兼容、渲染、状态机、Listener、View Model、WebAudio 桥接和 iOS Piano 短音播放 |
| `utils/rive-performance.js` | 小程序渲染质量、复杂度阈值和音频保护纯函数 |
| `utils/file-actions.js` | 发送文件与桌面端保存 |
| `components/parameter-tag` | 画板、状态机、时间轴和输入的统一标签 |
| `components/navigation-bar` | 首页与预览页的自定义顶部导航；按 Mac/Windows 胶囊位置补偿顶部空间，窗口缩放时重算，单页栈返回首页 |
| `assets/icons` | 从 `@phosphor-icons/core` 固化到包内的 Phosphor Bold SVG；由 `npm run vendor:icons` 统一生成颜色与来源标记 |
| `components/preview-panel` | 原生预览的唯一视图模板，同时服务手机全页与桌面宽屏分栏 |
| `utils/desktop-split.js` | 桌面端 `960px` 分栏阈值、平台判断和独立详情页回流首页的路由 |
| `vendor/rive` | 适配微信运行环境的完整 Rive Canvas 运行时、主版与兼容版两份 Brotli WASM |
| `h5/` | 独立浏览器 H5；默认使用 `@rive-app/webgl2-advanced@2.39.1`，失败时降级 `@rive-app/canvas-advanced@2.39.1`；以 IndexedDB 保存用户文件、输出实时 FPS 与当前会话事件，渲染和点击共用同一 Fit 矩阵 |
| `h5/lib/runtime-event-log.ts` | 当前文件事件的定长内存日志；微任务批量通知独立事件控制台，不上传、不持久化，也不推动根组件逐帧更新 |
| `shared/rive-policy.json` | 双端倍速、手势、遥测节流和复杂文件阈值的唯一真源；脚本生成微信可加载的 `rive-policy.js` |
| `AGENTS.md` | AI 修改边界、唯一真源、验证命令和包体预算 |

### 2026-08-17 整体重构结果

- H5 播放指标改为局部订阅，根组件不再承接时间轴/FPS 高频状态；元数据计算按 `100ms` 节流，画布拖拽期间不重建 backing canvas。
- Rive 播放器动态拆包，本地图标替代 `@phosphor-icons/react`。入口 JS gzip 从约 `108.7 KB` 降到 `69.7 KB`；播放器在打开文件时再加载，gzip 约 `29.4 KB`。
- H5 构建链收敛为 Vite，锁文件包数从 `741` 降到 `232`；本轮 Vite 构建实测约 `0.25–1.04s`，生产和开发依赖审计均为 `0` 漏洞。
- Rive WASM 改为版本化地址 `rive-2.39.1.wasm`，构建自动生成 Brotli `573,290 B` 与 Gzip `743,520 B` 文件，Nginx 使用一年 immutable 缓存。
- 小程序上传源包从 `1,837,997 B` 降到 `1,631,271 B`（`1593.0 KB`）；排除重复原始样例、测试和历史构建，并设置 `1,700,000 B` 自动预算。
- 好友分享图从 `126,297 B` PNG 改为 `37,141 B` JPEG；手机详情和桌面分栏共用一份 WXML/WXSS 模板。
- 预览页改为组件后，所有 `SelectorQuery` 都固定 `query.in(this)`；组件 WXSS 只使用 `:host` 与类选择器，避免开发者工具拒绝标签或属性选择器。

### 2026-08-17 真实文件与打包验证

- 微信开发者工具 RC `2.02.2607271` 普通编译通过，基础库 `3.17.0`；真实 `引导页动画750_1160.riv` 成功渲染，实测约 `21–23 FPS`，画板、状态机、时间轴与播放控件正常。
- 微信开发版二维码已成功生成，开发者工具显示代码包 `1646 KB`；“编译提示 160”全部是 `packOptions.ignore` 排除清单，不是编译错误。代码质量面板的“主包小于 1.5 M”建议仍未通过，原因是同时保留 SIMD 与兼容版 Rive WASM；当前仍满足微信上传限制和本项目 `1,700,000 B` 硬预算。
- H5 在 `http://127.0.0.1:4174/rive-viewer/` 本地生产模式启动，真实验证 `39 KB`、`79 KB`、`986 KB`、`2.3 MB`、`12.7 MB`、`55 MB` 六档文件；最大文件正常显示约 `22 FPS`，大文件加载后快速切换小文件不会被旧任务覆盖。
- 真实交互覆盖跟随角色指针、Piano 点击、Animojis `89` 画板延迟展开、Cosmic Game 状态机和 Room Decor 大文件。部分文件没有绑定当前画板 View Model，Rive 底层会输出兼容性提示，但渲染与交互不受影响。

### 2026-08-18 H5 正式部署

- 正式入口 <https://mikeywa.site/rive-viewer/> 已切换到版本目录 `/var/www/rive-viewer/releases/20260818-093940`，上一版 `20260813-215427` 保留为即时回滚点。
- 同步更新 `/etc/nginx/snippets/rive-viewer.conf`：HTML 不缓存，哈希 JS/CSS 与 `rive-2.39.1.wasm` 使用 Gzip 静态文件和一年 immutable 缓存；WASM 返回 `Content-Type: application/wasm`。
- 线上入口 JS、CSS、WASM 解压后 SHA-256 与本地生产构建逐字节一致，内置 `.riv` 样例文件头为 `RIVE`；Nginx 配置检查与服务状态均正常。
- 发布使用版本目录加 `current` 软链接原子切换。第一次验收因本机请求缺少域名命中默认站点而自动回滚，修正为带 `mikeywa.site` 的 HTTPS 验收后重新切换成功，期间旧站未丢失。

### 2026-08-25 JOJO 渲染兼容性修复与正式发布

- 正式页实测 `ai叫叫学伴.riv` 与 `ai叫叫学伴_源文件_绿豆.riv` 均能下载、解析、播放并读取状态机，但柔和内阴影和虚化边缘缺失；切换“高清”不恢复。
- 同一份绿豆文件使用同为 `2.39.1` 的 Canvas2D 与 WebGL2 渲染同一静态初始帧：Canvas2D 为硬边平涂，WebGL2 / Rive Renderer 能显示柔和体积光影。问题定位为渲染器能力边界，不是文件损坏、下载失败或清晰度档位。
- H5 已在本地改为默认 `@rive-app/webgl2-advanced@2.39.1`，Canvas2D 继续作为兼容模式；两套运行时保持动态拆包，分别使用 `rive-webgl2-2.39.1.wasm` 与 `rive-2.39.1.wasm`，避免旧 immutable 缓存冲突。
- WebGL2 初始化、逐帧运行或上下文失败会重建 Canvas、自动切换兼容模式，并显示约 `4.2s` 的“WebGL2 不可用，已切换到兼容模式”Toast。参数面板最后新增“渲染引擎”，可手动选择 WebGL2 或兼容模式。
- 本机真实绿豆文件已完成 WebGL2 / Canvas2D 双向切换和缺失 WebGL2 WASM 的失败注入；柔化光影仅在 WebGL2 恢复，兼容模式保持可播放。当前 JOJO 前端为 `/var/www/rive-host/releases/20260825-201557`，上一版 `20260825-192225` 与更早稳定版 `20260824-163850` 均保留回滚；线上双引擎分包、双 WASM 与本地产物逐字节一致。

### 2026-08-26 小程序回滚 Canvas

微信小程序真机无法正常加载 WebGL2 试验包，现已恢复 `<canvas type="2d">`、完整 `@rive-app/canvas-advanced@2.39.1` 与 `WXWebAssembly`。包内重新保留主版和兼容版两份 Canvas WASM，不包含 WebGL2 小程序运行时，也不提供小程序端引擎切换。微信开发者工具普通编译后，真实 `引导页动画750_1160.riv` 的画面、FPS、画板、状态机和时间轴均可见；H5 与 JOJO 版继续默认 WebGL2，不受本次回滚影响。

### 2026-08-25 普通 H5 默认高清与 WebGL2 正式发布

- 普通 H5 渲染质量默认值已改为高清；普通素材最高约 `60fps + DPR 2.0`，复杂文件仍由现有阈值自动限制到最高 `30fps + DPR 1.25`。
- <https://mikeywa.site/rive-viewer/> 已原子切换到 `/var/www/rive-viewer/releases/20260825-230508`，直接回滚点为 `20260818-093940`；HTML 预加载 `rive-webgl2-2.39.1.wasm`，浏览器仍保留 Canvas2D 兼容模式。
- 公网 HTML、入口 JS 和 CSS 的 SHA-256 与本地 `dist-static` 一致；WebGL2 与 Canvas2D 两份 WASM 均返回 `application/wasm + gzip + immutable`。内置浏览器桌面和 `360×800` 页面正常、无控制台错误。

### 复杂文件性能边界

- `.riv` 磁盘体积不等于运行复杂度；画板、嵌套实例、动画、状态机、矢量顶点、裁剪和持续 idle 才决定解析与逐帧成本。
- Animojis 测试文件为 `2.33 MiB`，但包含 `89` 个画板、`422` 条动画和 `86` 个状态机；默认 `3000 × 3000` 总览画板聚合整页表情，是当前真机性能样本。
- 首帧只解析并复用默认画板；其余画板由用户展开时分批读取，选中后才检查完整参数，避免首次进入同步创建大量运行时对象和标签。
- iOS 复杂无音频文件使用约 `24fps`、DPR 上限 `1.25`；暂停后停止持续绘制，交互、画布尺寸或参数变化时再按需重绘。
- 预览页提供性能、平衡、高清三档；H5 默认高清（最高约 `60fps + DPR 2.0`），微信小程序继续默认性能（约 `30fps + DPR 1.0`）并记住用户选择。复杂素材即使选择高清也保留自动安全上限。
- 微信未公开固定 CPU 或 iOS 内存配额，性能判断以目标真机分阶段计时、帧耗时和内存告警为准，不能用开发者工具流畅度代替。

Rive Web 运行时的通用音频链路通过兼容构造器桥接到 `wx.createWebAudioContext`。iOS Piano 使用专用资源加载器和 `BufferSource`，先显示首帧再按琴键优先顺序解码；单个解码超时会继续下一个，缺少琴键或解码失败时显式标记不可用，绝不回退到高负载混音。静音或离开预览会停止并释放音频；运行时只保留播放链路，不使用录音入口。

## 使用的微信 API

| API | 用途 |
| --- | --- |
| `wx.chooseMessageFile` | 从聊天文件批量选择 `.riv` |
| `wx.miniapp.chooseFile` | 支持环境中的系统文件多选 |
| `wx.getFileSystemManager` | 本地持久化、分块读取、封面和临时资源管理 |
| `wx.shareFileMessage` | 发送原始 Rive 文件给好友 |
| `wx.saveFileToDisk` | 微信桌面端保存文件 |
| `wx.showShareMenu` | 分享小程序卡片与朋友圈入口 |
| `wx.setClipboardData` | 仅在用户点击复制图标后写入网页版预览地址 |
| `wx.createWebAudioContext` | 播放 Rive 文件内嵌音频，不采集声音 |
| `WXWebAssembly.instantiate` | 加载 Rive WASM 运行时 |

小程序不再包含 `web-view` 页面，也不会自动打开或强制跳转外部网站。网页版地址只作为与复杂 Rive 预览直接相关的辅助信息放在首页底部，原生预览、导入和文件管理仍可独立完成。

微信小程序不调用 `wx.getClipboardData`，也不读取剪贴板已有内容；仅在用户点击网址旁复制图标后调用一次 `wx.setClipboardData`。公众平台用途统一填写：“用于用户主动复制网页版预览地址，方便在浏览器中打开复杂 Rive 文件；仅向剪切板写入固定网址，不读取剪切板已有内容。”项目检查会阻止剪贴板读取、后台写入和其他复制用途。独立 H5 只在用户主动点击底部反馈入口时调用浏览器 `navigator.clipboard.writeText` 写入作者微信号。聊天文件导入只处理用户主动选中的 `.riv` 文件，文件在设备本地解析、预览和保存，不上传业务服务器。

当前按 `project.config.json` 忽略规则估算的小程序 Canvas 源包为 `1,666,466 B`，共 `66` 个文件，并由 `npm run check:package` 强制不超过 `1,700,000 B`。包内保留主版和兼容版两份 Rive Canvas WASM；H5、原始样例、测试、历史构建和依赖目录均已排除。

首页和预览页均声明 `onShareAppMessage`、`onShareTimeline`，并在页面显示时调用 `wx.showShareMenu`。好友卡使用 `share-friend.jpg`（500×400，约 37 KB）；朋友圈使用 `share-timeline.png`（272×272，约 14 KB）。若运行时返回 `showShareMenu:fail no permission`，应在公众平台检查账号权限，代码不能绕过平台拒绝。

本项目是微信原生小程序，不使用 Taro 的 `enableShareAppMessage` / `enableShareTimeline` 编译提示。微信原生页面与全局配置文档未列出这两个字段；本项目加入字段并重新编译实测后，分享菜单和 `no permission` 结果均无变化，因此不保留无效配置。

## 本地开发

```bash
npm ci
npm run vendor:rive
npm run check
```

随后在微信开发者工具中导入项目根目录。`project.config.json` 当前绑定项目 AppID；复用代码时请替换为自己的 AppID。真机系统文件选择能力取决于微信版本与平台，聊天文件导入始终作为基础入口。

常用检查：

```bash
npm run check
npm run verify:interactions
npm run benchmark:rive
```

本轮微信开发版预览产物位于 `dist/wechat-preview-2026-08-17/`，包含二维码、开发者工具完整截图和生成信息。二维码是微信短期开发版凭证，会按 `preview-info.json` 中的时间失效；过期后需在开发者工具重新点击“预览”。打包会排除 H5、测试素材、依赖目录、脚本、本地配置与项目文档。

同页分栏目前只在 Mac、Windows 或开发者工具的窗口宽度达到 `960` 个逻辑像素时启用；低于阈值仍使用原生 `navigateTo` 详情页。Retina 屏幕的物理像素不能直接视为小程序逻辑宽度。

`rive测试文件/` 是本机性能与兼容性素材目录，已从公开仓库排除。小程序实际内置资源位于 `assets/samples/`。

网页版与原生小程序文件库相互独立：H5 不再预置示例，也不会自动读取原生小程序已导入的文件。旧 `/rive-viewer/` 的导入只写入当前 Origin 的 IndexedDB；托管根域名的“上传 Rive 文件”入口会把用户主动选择或拖入的文件自动上传，已有本地文件也可从文件菜单补传并生成链接。

预览器只接收 Rive Runtime 导出的 `.riv` 二进制文件。`.rev` 是 Rive 编辑器的备份/工程文件，包含运行时导出会剥离的编辑信息，需先在 Rive 编辑器中导入并重新导出为 `.riv` 后预览。

### 独立 H5

```bash
cd h5
npm ci
npm run dev
npm run build:static
```

本地入口为 <http://localhost:5173/rive-viewer/>，旧本地预览正式入口为 <https://mikeywa.site/rive-viewer/>。默认构建基址仍是 `/rive-viewer/`；托管域名使用 `RIVE_VIEWER_BASE=/ npm run build` 生成根路径产物。两者都输出 `h5/dist-static/` 并预压缩 JS、CSS、SVG 与 WASM，部署时分别使用独立版本目录和 `current` 软链接。

H5 首页在各宽度下都保持原有纵向布局。桌面宽度达到 `860px` 后，详情页左侧保留 `184px` 的统一最近文件栏，顶部“＋”继续选择上传；黄色按钮内使用近黑色加号，当前文件使用独立的 `3px` 圆角黄色竖线强调。移动端仍使用独立详情页，不显示窄栏。详情左上角返回按钮与 `Esc` 都回到首页文件列表，公开链接在加载、失败、归档和评论输入聚焦状态也保持一致。首页右上角保留键盘按钮，集中说明 `R` 重播、空格播放/暂停、方向键切换文件、`+/-` 调速和 `Esc` 返回；详情页则只保留下载与复制分享链接两个文件操作。文件首次成功渲染后保存 `160×120` 低清 WebP 首帧封面，之后列表无需重新解析即可立即显示；公开文件的封面同样只保存在当前浏览器。文件操作箭头在鼠标悬停、键盘聚焦或菜单展开时出现。标题旁以 `9px` 小字显示 `for JOJO`，该托管版本也称“JOJO 版”或“叫叫版”；页面底部只显示纯文本“反馈意见：杨皓棱”。浏览器页签与左上角标题共用 Figma 羊石图标，同时提供 PNG 回退、WebP 图标和缓存版本号。

H5 同步小程序近期交互：首页主要点击入口使用中心放大回弹；托管根域名支持首页和详情页点击或拖入上传，首个 `.riv` 保存后立即从本机打开，预览与云端上传并行。公开中、最近查看和本机文件合并为一个按最近打开、评论或上传时间排序的列表，不再分“托管文件 / 最近文件”；但进入详情后，左侧列表切换文件只更新选中态，不再改变当前顺序或自动展开上传状态。体积后依次用云勾、电脑、上云箭头或失败图标表达状态；上传中与失败在该文件行内展开进度、错误和重试，不再显示独立完成卡片。旧 `/rive-viewer/` 只保存到当前浏览器并隐藏公开链接入口。本地文件菜单保留“上传并生成链接 / 复制公开链接”和“发送文件”，不提供不可恢复移除。系统支持时“发送文件”调用 Web Share 文件面板，否则回退下载。画布手柄单击展开原位“完整 / 铺满”，按住或拖动时高亮并支持滑选；事件控制台位于参数区第一栏，公开评论紧跟其后，缩放方式位于最底部，背景色块为 `45×30px` 短胶囊。

H5 的“完整”模式会让预览画布按当前 Rive 画板宽高比居中收拢，在可用区域内保持比例，不再强制左右撑满；“铺满”仍使用整个可用画布。JOJO 托管版首次打开文件时默认使用“完整”，旧 `/rive-viewer/` 本地版仍默认“铺满”。详情参数区不再显示“文件操作”。托管文件详情在手机顶部栏与宽屏文件标题区都只保留下载和黄色“复制分享链接”；复制成功后按钮原位切换为普通深色样式、打钩图标与“已复制”，约 `2.2s` 后复位，不再打开详情分享弹层。旧本地入口仍只显示下载。文件列表的 Hover 背景由整行承接，连续覆盖到最右侧下拉箭头区域。

公开链接在新浏览器中会完整下载 `.riv` 后再解析。`getHostedFile` 使用 `ReadableStream` 按字节读取并展示真实百分比；20 秒未收到响应或 30 秒无新字节时进入可重试错误，不再无限停在“正在打开”。详情侧栏切换未缓存的云端文件时使用页面内路由，原详情结构保持可见，文件名读取后只在预览画布内显示下载和解析 Loading。下载完成后立即进入播放器，最近查看记录在后台保存，不再阻塞首帧。

当前生产主机、Node 与 Nginx 均未设置 GET 下载速度上限；服务器内部读取 `17,590,968 B` 的 `RFS` 约需 `0.03–0.10s`。2026-08-24 公网直连、代理与 SSH 实测长期吞吐约 `1–2 Mbps`，高度指向腾讯云公网带宽规格或云出口 QoS，代理只会额外造成波动。精确套餐值以腾讯云控制台为准；若长期托管 10–60 MB 文件，应提高公网带宽或把不可变 `.riv` 本体迁到腾讯云 COS/CDN。

H5 文件列表底部将“小程序”入口和纯文本“反馈意见：杨皓棱”并排展示；反馈文字不再是按钮，不提供复制行为。“小程序”使用 `WechatLogo`，悬停、键盘聚焦或触屏点击后，在入口上方展示“Rive 预览台”名称与本地小程序码。小程序码使用 `320×320` WebP，约 `32 KB`，不依赖外部图片服务。

### JOJO 横屏工作台

JOJO 托管版在浏览器宽度达到 `1194px`（iPad Pro 11 寸横屏）时使用可调双栏：左侧画布默认占满可用视窗高度并保持 Rive 文件原始宽高比，画布下方手柄继续支持手动调高；桌面优先把画面拉到约 `800px` 宽，再把剩余空间分给最小 `360px` 的右侧检查栏，中缝可用鼠标或键盘调整。左侧最近文件栏默认 `184px`，可在 `160–360px` 间拖动或键盘调整；文件名 Hover 或聚焦时在栏外浮层显示完整名称。低于该宽度保持上下布局。

评论输入框默认一行、按钮内置，按钮层级高于可编辑区；普通回车直接发表，`Shift + Enter` 或 `Command + Enter` 换行，中文输入法组字期间不误发。空白时使用 Calligraph 轮换显示评论提示；编辑时点击任意时间轴，会在当前光标位置插入带下划线的完整名称，一条评论可嵌入多个时间轴。时间轴支持“展开 / 整理”：超过 `10` 条时首次默认进入“整理”，`10` 条及以下默认“展开”，之后仍可手动切换；整理时无一级前缀的项目排最前，重复下划线前缀收为组标题，标签只显示后缀但评论仍写入完整名称。超过 `10` 条时同时收紧标签内边距。事件、时间轴标题与当前组标题按顺序置顶，Hover 标签会高亮对应组标题。自定义倍速菜单替代浏览器原生控件；暂停时同时暂停 Rive 内部音频。时间轴默认整理规则已随 JOJO 前端 `20260825-225304` 正式发布；按钮命中、评论快捷键与文件页内加载修复已随 `20260826-084827` 正式发布。

## Rive 托管平台

正式入口为 <https://rive.mikeywa.site/>。用户在首页或详情页主动选择或拖入 `.riv` 后，文件先保存到当前浏览器并立即打开，再与预览并行顺序上传到云端；每个文件行显示真实百分比、失败重试或云端完成图标，三位短码与复制入口位于文件菜单。旧入口 <https://mikeywa.site/rive-viewer/> 保持纯本地预览版，避免旧 Origin 的 IndexedDB 文件丢失。

- 公开页为 `/<3 位短码>`；短码使用大小写敏感 Base62，`api` 为保留码，服务端生成时跳过并检查冲突。旧 `/s/<短码>` 使用 `308` 跳转到新地址。分享页复用 `WebRivePlayer`、`PlaybackTelemetry` 和完整 Rive WASM。
- 无用户系统，所有访问者权限相同。公开页可播放、下载、复制链接并提交纯文本评论；详情页右上角直接复制当前三位公开链接。每个三位短码拥有独立评论区，切换文件时先切换评论线程再读取目标文件评论，不复用上一文件的列表。评论只保留一个正文输入区，不要求填写名字。浏览器首次评论时生成并保存随机访问标识，服务端据此稳定分配 32 组“萌萌森林”动物昵称和头像；同一浏览器再次评论时保持不变，无法保存标识时才使用 IP 与 User-Agent 的即时哈希兜底，不保存原始 IP。头像以圆形裁切的 `32×32px` 显示在昵称前。评论可由任何人归档，归档后默认折叠，展开可查看原文并恢复。评论列表在公开元数据确认后与文件下载并行读取，读取失败与发表失败分别处理。服务端公开中、本机导入、成功查看或评论过的非示例公开文件合并为一个“最近文件”列表，首页按最后打开、评论或上传时间排序；详情侧栏切换选中项时保持当前顺序。列表复用本地 `160×120 WebP` 缩略图，只缓存远端元信息和封面，不长期保存整份远端 `.riv`。已归档文件不在首页展开，只通过“已归档”小按钮打开弹窗恢复；数量为无底色、无黄色强调的普通灰色数字。
- 文件只做“归档 / 恢复”，不提供永久删除。归档前二次确认；归档后原链接停止播放和新增评论，恢复后沿用原短码、评论和创建时间。
- 萌萌森林头像母版位于 `h5/assets/forest-avatars-master-4k.png`，尺寸为 `3840×1920`；母版是 `8×4` 方格，每格严格 `480×480px`。运行时使用 `h5/public/rive-viewer/avatars/` 下 32 张一一对应的不透明方形 WebP，单张严格为 `32×32px`；每张使用不同满版底色和大动物轮廓，由 `h5/scripts/build-forest-avatars.py` 按比例格线生成，避免透明边缘细缝和二次裁偏。
- 后端为 `server/` 下仅使用 Node 内置模块的单进程服务，监听 `127.0.0.1:8097`。文件保存在 `/var/lib/rive-host/files/`，状态以 `state.json` 原子更新，发布目录与数据目录分离。
- 单文件上限 `64 MiB`、托管总量 `5 GiB`、最多 `10,000` 个分享；磁盘可用空间低于 `6 GiB` 时拒绝上传。单文件最多 `500` 条评论，全站最多 `5,000` 条评论。
- 每次上传都生成新的三位短码和独立评论区，即使文件字节完全相同也不复用旧短码；SHA-256 只用于完整性、ETag 和示例导入识别。Nginx 对上传和写操作按来源限速，归档与恢复要求 `X-Rive-Action` 确认头，页面和响应带 `noindex`。

### 托管 API

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/healthz` | 服务健康检查 |
| `GET / POST` | `/api/v1/shares` | 按状态列出文件；上传原始 `.riv` |
| `GET` | `/api/v1/shares/:code` | 获取分享元数据 |
| `GET / HEAD` | `/api/v1/shares/:code/file` | 下载文件，支持 Range 与 ETag |
| `GET / POST` | `/api/v1/shares/:code/comments` | 读取和提交纯文本评论 |
| `POST` | `/api/v1/shares/:code/comments/:commentId/archive` | 归档评论，要求确认头 |
| `POST` | `/api/v1/shares/:code/comments/:commentId/restore` | 恢复归档评论 |
| `POST` | `/api/v1/shares/:code/archive` | 二次确认后归档 |
| `POST` | `/api/v1/shares/:code/restore` | 恢复原分享链接 |

生产发布使用 `/opt/rive-host/releases/<时间戳>`、`/var/www/rive-host/releases/<时间戳>` 与原子 `current` 软链接；systemd 使用动态低权限用户，Nginx 只公开 HTTPS 和 `/api/` 代理。证书由 Certbot 自动续期，续签成功后会检查并重新加载 Nginx。

## 黑客松展示素材

- 包装文案统一维护在 `HACKATHON-2026.MD`，主叙事为“别再发录屏了。一个链接，直接看 Rive”。
- 真实展示样例为 <https://rive.mikeywa.site/vGd>；宣传只描述“适合在钉钉会话中分享的 H5”，不声称钉钉原生登录、机器人或微应用能力。
- 6 张 `1536×1024`、`3:2` 宣传封面位于 `h5/assets/hackathon/posters/`，均基于 `vGd` 真实桌面/手机页面与绿豆角色生成；`h5/assets/hackathon/validation/` 保留安全区和 `390×260` 手机缩略检查图。

## 许可证

本项目中由仓库作者原创的源代码依据 [PolyForm Noncommercial License 1.0.0](LICENSE) 提供。你可以为非商业目的使用、修改和分发；任何商业用途均须事先取得版权方的单独书面授权。具体权利和义务以 `LICENSE` 正文为准。

因此本仓库属于“源码公开（source-available）”，不是 OSI 定义下的开源软件。第三方依赖和示例资源不适用项目许可证，详见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
